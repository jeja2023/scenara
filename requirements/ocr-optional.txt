# OCR 领域的可选推理运行时，不进入 requirements/production.lock。
#
# 版本锁在 PaddleOCR 2.x：scenara/domains/ocr/operators.py 的 PaddleOcrEngine 使用
# use_angle_cls / show_log / det_model_dir 构造参数与 ocr(img, cls=True) 调用形式，
# 这些在 PaddleOCR 3.x 已被移除，升级前必须先改适配层。
#
# PaddleOCR 2.9.1 的适配器使用旧版 PaddleOCR API。Linux Python 3.12 的 GPU wheel
# 使用 Paddle 官方 CUDA 11.8 的 2.6.2 构建；Dockerfile 用同一地址构建 OCR 专用 Core
# 镜像。若目标 CUDA/架构不同，替换为匹配官方 wheel 后重新构建，不要把 Windows wheel
# 复制到 Linux 容器：
#
#   pip install https://paddle-whl.cdn.bcebos.com/stable/cu118/paddlepaddle-gpu/paddlepaddle_gpu-2.6.2-cp312-cp312-linux_x86_64.whl
#
# 模型权重仍须走经批准的离线模型资产流程提供。
paddleocr==2.9.1
