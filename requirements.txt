# Scenara 核心运行时依赖。
#
# 这份清单经 requirements/production.in 编译进 requirements/production.lock，
# 是生产镜像唯一的安装来源，因此只收纳平台内核必需的包。领域推理运行时（PaddleOCR、
# PaddleVideo、PyTorch）体积在 GB 级且与部署的 CUDA 版本强绑定，放在
# requirements/ocr-optional.txt 与 requirements/domain-optional.txt 中按需安装。
fastapi==0.139.0
uvicorn[standard]==0.51.0
onnxruntime-gpu==1.20.2
numpy==1.26.4
opencv-python-headless==4.10.0.84
pillow==12.3.0
pypdfium2==5.4.0
pydantic==2.13.4
python-multipart==0.0.32
python-dotenv==1.2.2
PyYAML==6.0.3
cryptography==50.0.0
httpx==0.28.1
tzdata==2026.3; sys_platform == "win32"
