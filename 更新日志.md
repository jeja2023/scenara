# Scenara 景枢更新日志

## 2026-08-31：0.3.0-dev.38 接入与权限产品目录标准化与 9 大核心产品对齐、弹窗体验扩容升级与全链路中文化

本版本启用 `0.3.0-dev.38`，重点聚焦于接入与权限（IAM）产品目录标准化与 9 大核心产品对齐、全站弹窗体验扩容与自适应展示优化、弹窗交互统一（移除冗余关闭按钮）、权限标识列纯净视觉重构以及前后端全链路 UI 中文化。

### 主要成果

- **接入与权限产品目录标准化与 9 大核心产品对齐**：
  - 彻底梳理并标准化平台 9 大核心产品定义：`parse`（智能视觉与文档解析）、`search`（综合与多模态检索）、`model`（AI 模型管理与准入）、`data`（数据资产与数据集治理）、`flow`（智能分析流水线编排）、`index`（特征与向量索引底座）、`api`（开放应用接口）、`sdk`（多语言开发工具包）、`console`（平台共享控制台）；
  - 排除并过滤非独立子领域（`train`, `surveillance`, `media`）及实验门禁产品（`edge`, `agent`），确保项目产品授权、角色定义、服务账号与 API 密钥授权范围精准清晰；
  - 在 `scenara/platform/product_catalog.py` 与 `labels.ts` 中对齐并规范所有产品的中文名称、领域说明、职责摘要与能力范围清单，汉化并规范了 `OCR 文档解析与识别`、`Qdrant 向量特征库多租户适配`、`OpenAPI 标准契约`、`v1 接口路由`、`Webhook 事件推送`、`Python/TypeScript SDK 客户端` 等核心能力项。
- **弹窗尺寸扩容与排版自适应优化**：
  - 普通中型弹窗（如新建组织、新建项目、新建用户、向项目分配成员）宽度由 `520px/620px` 升级为 `780px`（`min(780px, 95vw)`）；大型配置弹窗（如新建服务账号、签发 API 密钥、保存产品授权、新建安全角色）宽度由 `720px` 扩容至 `980px`（`min(980px, 96vw)`）；
  - 优化角色、服务账号、API 密钥弹窗中的产品范围选择器，由原来的自适应拥挤窄列调整为 **标准的 3 列卡片网格（3×3 布局）**，单项宽度扩充至 `290px+`，如「数据资产与数据集治理」、「智能分析流水线编排」等长中文产品名称与领域副标题均**单行完整显示、不换行不溢出**；
  - 将「保存项目产品授权」弹窗原有的普通下拉选择升级为 3×3 可视化产品选择卡片网格，展示中文名、产品标识、摘要说明与核心能力标签；移除了卡片网格 `max-height: 290px` 强制滚动高度限制，9 大核心产品**一览无余平铺展示**，彻底消除内部多余垂直滚动条。
- **弹窗交互规范化与关闭操作去重**：
  - 检查并移除了「接入与权限」页面全部 9 个模态弹窗右上角与右下角「取消」按钮功能重复的关闭图标按钮（`modal-close-btn`），统一支持点击遮罩外层（Overlay）及底部明确的「取消」按钮关闭，界面更加整洁统一；
  - 修复向当前项目分配成员与角色弹窗中「用户」与「服务账号」分段控制器的宽度自适应与 50%/50% 均匀分配，消除右侧大块空白区域。
- **表格标识列纯净视觉重构**：
  - 优化了 `.muted-id` 样式类，移除了 `background: #f1f4f3` 和内边距，避免将表格中「租户标识」、「项目标识」、「用户标识」、「角色标识」等整列单元格渲染为突兀的灰色块，保持清晰优雅的等宽排版。
- **跨浏览器与标准 CSS 兼容性加固**：
  - 修复多处多行文本截断中缺少标准 `line-clamp` CSS 属性的警告，确保在最新现代浏览器与多平台渲染引擎中表现稳健。
- **版本对齐**：Python 主包与 SDK 升级为 `0.3.0.dev38`；Node 根包、Console、TypeScript SDK、OpenAPI、Compose、Kubernetes 与离线包标签统一升级为 `0.3.0-dev.38`。

## 2026-08-31：0.3.0-dev.37 控制台界面体验优化与视觉一致性升级、布控预警系统深度重构与数据资产视图标准化

本版本启用 `0.3.0-dev.37`，重点聚焦于控制台全站界面体验优化与视觉一致性升级（顶栏精简与全宽页面视野释放、全局切换按钮淡绿选中态统一）、布控预警系统全面中文化与去重重构、数据资产过滤搜索与 Tab 分段视图标准化，以及全局分页组件稳固性治理。

### 主要成果

- **顶栏精简与全屏空间扩展**：
  - 移除了顶部导航栏各页面左侧冗余的“视觉 AI 中枢平台”文字徽标与分隔线，让操作界面更清爽聚焦；
  - 清理并移除了页面左右两侧的历史边距限制，解除 `max-width` 约束，将 `.page`、`.search-page`、`.results-page` 容器宽度扩展为 100%，大幅释放横向空间，显著提升表格与多媒体视图的有效操作视野。
- **布控预警系统全面中文化与去重重构**：
  - 针对布控预警（实时预警中心、告警研判、布控任务、布控名单）完成前端 UI 界面深度中文化，在 `labels.ts` 中补齐了告警研判状态（待处置/已确认/误报/已忽略）、任务状态（草稿/运行中/已暂停/已停止/已归档）、名单类别（黑名单/白名单/自定义）、名单状态（已启用/已停用）与模态类型的中文映射；
  - 移除了布控预警各子页面内 Tab 导航下方与顶部页面标题重复的标题名称与注释说明；
  - 实时预警页面的连接状态与静音控制工具栏优化为紧凑右对齐；告警研判、布控任务与布控名单页面的“刷新”按钮统一就近整合至筛选栏与列表面板头部；
  - 重构了 `SurveillanceLayout.vue` 的 Tab 导航，移除了原来突兀的独立外框卡片与投影，采用全局标准的嵌入式分段 Tab 栏（浅灰底色 `#eef2f1`、轻量圆角 `6px`、标准图标尺寸 `13px`）。
- **全局切换按钮（Tab / Segmented）选中态视觉统一**：
  - 参照布控预警页面的选中高亮样式，将全站所有页面切换按钮选中的白色背景（`#ffffff`）统一替换为淡绿色高亮背景（`var(--color-accent-soft, #e4f1f1)`）与深青/绿文字（`var(--color-accent-hover, #065e67)`）；
  - 统一覆盖了多模态解析工作区（能力模式、数据类型、上传来源）、综合检索模式切换（文搜图/人搜图）、总览多维矩阵切换、模型引擎领域切换、用户反馈领域切换、权限治理领域切换、结果详情抽屉 OCR Tab 等。
- **数据资产页面 UI 重构与 Tab 分段隔离**：
  - 彻底废弃文件资产原有的多组分段按钮，改用全局统一的 `<div class="filter-controls">` 过滤搜索栏，配备漏斗图标“筛选记录”标识、领域下拉选择框（全部领域/服饰风格/人像解析/行为分析/文字识别）、类型下拉选择框（全部类型/图片/视频/文档）与一键重置按钮，并保留匹配数量与文件总容量统计；
  - 在“数据资产”页面顶部新增全局统一样式的 Tab 切换导航（“文件资产”与“视频流源”，附带数量角标），将垂直堆叠的两大面板解耦为独立 Tab 页面，彻底解决长页面上下翻动问题。
- **全局分页控件换行与尺寸溢出治理**：
  - 为 `DataTablePagination.vue` 与全局 `styles.css` 中的 `.pagination-jumper`、`.jumper-label` 和 `.pagination-controls` 增加 `white-space: nowrap` 与 `flex-shrink: 0` 规则，防止“跳至”等中文文本在小屏或窄容器下发生异常换行；
  - 隔离了全局通用输入框样式对分页跳页输入框的样式污染，精确固定 `.pagination-jumper-input` 尺寸为 `40px x 24px`、行高 `22px`、内边距 `0 4px`，隐藏数字输入框自带的上下微调箭头（Spinner），确保跳页输入框在任何屏幕宽度下均保持紧凑精致且不溢出分页容器。
- **版本对齐**：Python 主包与 SDK 升级为 `0.3.0.dev37`；Node 根包、Console、TypeScript SDK、OpenAPI、Compose、Kubernetes 与离线包标签统一升级为 `0.3.0-dev.37`。

## 2026-08-30：0.3.0-dev.36 比对预警与布控系统落地、控制台全局表格重构与本地演练能力增强

本版本启用 `0.3.0-dev.36`，重点落地了比对预警与黑白名单布控系统（包含顶层三阶段研发计划、领域数据模型、1:N 向量比对、滑动频控去重、内存/Postgres 双持久化、全套控制台视图与演练套件）、控制台全站数据表格标准行高与布局深度重构，以及平台原生备份恢复与数据切流等多场景本地演练脚本套件。

### 主要成果

- **比对预警与布控系统完整落地**：
  - 制定并交付《Scenara 比对预警与布控功能研发计划与任务分解》（`docs/SURVEILLANCE_ALERT_PLAN.md`），系统规划了核心底座与引擎 (Phase 1)、控制台可视化与研判 (Phase 2)、时空轨迹联动与自闭环 (Phase 3) 的清晰演进路线；
  - 落地布控名单库（`Watchlist`，支持黑名单/白名单/自定义组）、布控目标档案（`WatchlistTarget`）、布控任务（`SurveillanceTask`）与实时预警记录（`AlertRecord`）等核心领域模型；
  - 实现基于 `FeatureStore`（pgvector / Qdrant）的 1:N 向量 Top-K 快速检索与人脸 + 人体 ReID 多模态加权融合比对；
  - 建立基于 `(target_id, camera_id)` 冷却窗口的滑动频控与去重抑制机制（Debounce），有效防止同一目标在摄像头前停留时引发告警风暴；
  - 提供内存态与 PostgreSQL 双存储持久化（`memory_surveillance.py`, `postgres_surveillance.py`）并配套数据库迁移脚本（`0014_surveillance_alerts.sql`, `0015_surveillance_debounce_aggregates.sql`）；
  - 扩展 Webhook 事件支持 `alert.triggered` 与 `alert.triaged`，并支持告警人工研判处置流（`confirmed` / `false_positive` / `ignored`）；
  - 控制台前端交付全套布控管理、布控任务调度、实时预警大屏、告警历史与人工研判看板页面（`frontend/console/src/views/surveillance/`）；
  - 提供本地布控与告警全流程演练脚本 `scripts/run_surveillance_local_simulation.py` 与自动化测试 `tests/test_surveillance.py`。
- **控制台全站表格与数据视图深度重构**：
  - 全面重构并优化了解析结果（`ResultsView.vue`）、长期轨迹（`TrajectoryView.vue`）、数据集治理（`DatasetView.vue`）、接入与权限（`AccessView.vue`）、资源与安全（`GovernanceView.vue`）等业务页面的表格呈现；
  - 数据表格单元格严格单行展示，彻底消除垂直挤压与不规则折行，严格对齐全局 28px 标准行高与边框规范；
  - 优化解析结果列宽分配，收紧解析成果概况列宽度至 200px，释放剩余横向空间给资源名称列；
  - 修复 `TrajectoryView` 泛型类型报错并清理 `DatasetView` 未使用的 `Database` 导入，全面统一全局 Tabs、紧凑按钮与分页控件规范。
- **平台原生备份恢复与多场景本地演练套件增强**：
  - 新增本地原生备份恢复演练脚本 `scripts/local_native_backup_restore_drill.py`；
  - 新增数据平台切流仿真演练脚本 `scripts/run_local_data_cutover_simulation.py`；
  - 新增证据流仿真脚本 `scripts/run_local_evidence_pipeline_simulation.py`；
  - 新增流式协议仿真演练脚本 `scripts/run_local_stream_protocol_simulation.py`。
- **版本对齐**：Python 主包与 SDK 升级为 `0.3.0.dev36`；Node 根包、Console、TypeScript SDK、OpenAPI、Compose、Kubernetes 与离线包标签统一升级为 `0.3.0-dev.36`。

## 2026-08-28：0.3.0-dev.35 人像零截断全量检出、二次元战袍/洛丽塔精准识别、全平台数据表格分页与跨镜轨迹冒烟套件

本版本启用 `0.3.0-dev.35`，重点落地了人像全量无损检出（彻底移除检测上限截断限制）、服饰风格识别增强（攻克暗黑系二次元战袍、黑发角色与洛丽塔裙装精准召回，并保护日常通勤着装防误判）、全平台通用数据表格分页控件与每页条数自选限制支持，以及跨镜长期轨迹系统的全面深入审查与专属快速端到端冒烟测试套件交付。

### 主要成果

- **人像全量检出与零截断限制解除**：
  - 彻底移除了人像分析在检测框后处理阶段历史遗留的 `MAX_DETECTIONS = 64` / `MAX_TRACKLETS = 64` 硬编码截断限制；
  - 算法全面升级为动态可配置检测上限（默认最大支持单帧 1000+ 个人员全量无损检出），解决密集人流、大型集会或远景画面中由于数量阈值导致部分人体未被框选漏检的问题；
  - 在后处理、多人员追踪与人体/人脸特征提取链路（`app/postprocess.py`, `app/runtime_body.py`, `app/runtime_face.py`）中全链路打通无限制高并发检测。
- **服饰风格识别增强与二次元 Cosplay / 洛丽塔精准识别**：
  - 增强了二次元角色、ACG 战袍、洛丽塔等小众风格的检测召回率，支持暗黑系战袍、深发色 ACG 角色（如黑发战袍、铠甲披风、短裙佩刀）与洛丽塔裙装（Lolita White Dress）；
  - 优化了色彩直方图、边缘纹理与特征加权算法，同时引入负样本对比防护机制，严格保护日常休闲通勤服饰（Polo 衫、普通黑色外套配牛仔裤、白T恤配黑色短裤等），防止误判为战袍；
  - 实现了多风格精准分类：Cosplay（二次元战袍/角色）、Lolita（洛丽塔）、Hanfu（汉服/国风）、JK（JK制服）、Gothic（哥特风）、Qipao（旗袍）、Techwear（机能风）等。
- **全平台数据表格通用分页控件与每页条数限制支持**：
  - 彻底解决了解析结果详情抽屉、解析结果页面等数据表格无分页、条数过多铺展过长的问题；
  - 重构升级了 `DataTablePagination.vue` 与 `DataTable.vue`，新增每页条数自选下拉选择器（10 / 20 / 50 / 100 条/页，切换条数自动平滑回到第 1 页）、高光数字页码、智能省略导航与快捷跳页输入框；
  - 提供开箱即用的**自动客户端内存切片分页**与**服务端异步分页**双模式支持；
  - 在当前单元目标明细表（`selectedUnit.objects`）、通用领域结构化扩展表（`GenericDomainResult.vue`）、解析结果列表（`ResultsView.vue`）、任务运行列表（`RunsView.vue`）中全量应用。
- **跨镜头长期轨迹深入检查与专属快速冒烟测试套件**：
  - 深度审查并验证了跨镜头多模态 Re-ID 融合（OSNet 人体 55% + ArcFace 人脸 45% 加权）、时空拓扑约束（最小/最大物理耗时瞬移阻断）、同帧防自碰撞排他、32个动态模板池 LRU 衰减淘汰，以及身份合并（Merge）/片段拆分（Split）治理全流程；
  - 编写并交付了长期轨迹专属快速端到端冒烟测试脚本 `scripts/test_trajectory_quick.py`（5大核心模块 100% 通过，耗时 0.34s）；
  - 平台 82 项自动化测试（`pytest`）及 25 项长期轨迹专项测试 100% 通过。
- **版本对齐**：Python 主包与 SDK 升级为 `0.3.0.dev35`；Node 根包、Console、TypeScript SDK、OpenAPI、Compose、Kubernetes 与离线包标签统一升级为 `0.3.0-dev.35`。

## 2026-08-27：0.3.0-dev.34 OCR 分层合规审查、工作台布局深度重构与交互体验极简化

本版本启用 `0.3.0-dev.34`，重点落地了 OCR 企业级分层合规审查风控引擎（支持极限词/敏感词动态注入与白名单豁免）、多模态解析工作台深度布局重构与极简交互优化（消除视频下方冗余面板、布尔参数纯色块药丸化、所有参数默认直展、文档核心参数三列同行、语言提示升级为下拉选择框、选择文件按钮质感优化与资产库默认不选中治理）。

### 主要成果

- **OCR 分层合规审查与企业级风控词库体系**：
  - 深度重构 OCR 文本合规性检测逻辑（`compliance.py`），支持法律法规强制性红线（《广告法》绝对化极限词、涉政违规、虚假承诺与诱导）与企业可定制风控词库双层结合；
  - 增加**自定义敏感词**（`custom_sensitive_words`）与**合规豁免白名单**（`compliance_whitelist`）动态配置通道，支持通过流水线领域参数实时下发或挂载本地 `data/compliance/custom_sensitive_words.json`；
  - 实现白名单优先放行判定机制（如“第一人民医院”、“第一分公司”、“国家级风景名胜区”等合法机构名精确豁免报错），杜绝合法词汇被极限词误杀；
  - 违规事件支持严重程度分级管控（`block` 违规阻断、`warning` 风险警告、`info` 合规提示），并随分析结果返回精准法条参考依据与修改建议。
- **多模态解析工作台深度布局重构与极简化**：
  - **消除冗余与操作混淆**：彻底移除视频播放画板下方的额外指标面板与多余的“开始解析”按钮，全工作台统一保留右上角唯一的全局 `[ 开始解析 ]` 主操作入口，界面高度整洁聚焦；
  - **领域布尔参数纯色块标签化**：将原 Checkbox 复选框方案重构为现代语义化纯轻量药丸胶囊按钮（Pill Button），彻底移除方块勾选框 `[✓]`；未选中为中性浅灰，选中即通体高亮翡翠绿（`#ecfdf5` + `#10b981`），点击交互灵动高级且全页面通用生效；
  - **取消参数折叠功能，默认直接平铺展开**：彻底移除所有“展开/收起高级参数”折叠切换按钮，采样参数与领域配置均平铺直展，免除二次点击展开的操作成本；
  - **文档解析核心参数三列同行排布**：采用动态三列栅格（`cols-3`），将 `最大页数`（100）、`最低置信度`（0.5）与 `语言提示` 紧凑对齐在首行同一行；长文本项 `合规豁免白名单` 与 `自定义敏感词` 跨满整行（`parameter-wide`）；图片/视频模式下自适应为双列并排（`cols-2`）；
  - **语言提示升级为标准下拉选择框（Select）**：彻底免去手动敲击 `zh`、`en` 等简码的认知负担，内置中英通用 (zh)、英文 (en)、日文 (ja)、韩文 (ko)、繁体中文 (cht)、法语 (fr)、德语 (de)、俄语 (ru)、西班牙语 (es) 等标准语言选项，直观好用；
  - **OCR 介质标签顺序规范化**：调整为符合业务工作流的标准顺序：**`[ 图片 ]` $\rightarrow$ `[ 文档 ]` $\rightarrow$ `[ 视频 ]` $\rightarrow$ `[ 视频流 ]`**，并在插件清单中同步更新；
  - **选择文件按钮质感优化**：在严格保持原有紧凑规格尺寸（`26px` 高度）前提下，针对 `::file-selector-button` 重设现代圆角微投影与翡翠绿悬停交互，消除原生浏览器粗糙灰色斜面边框；去除未选中时的冗余文字，已选文件优雅展示绿色文件大小提示；
  - **资产库刷新与页面切换默认不选中**：页面刷新（F5）或切换页面（路由/领域/介质变更）后，资产库下拉框严格保持空白未选中占位状态（“选择图片/文档/视频”），并切断本地上传对资产库下拉状态的隐式污染。
- **版本对齐**：Python 主包与 SDK 升级为 `0.3.0.dev34`；Node 根包、Console、TypeScript SDK、OpenAPI、Compose、Kubernetes 与离线包标签统一升级为 `0.3.0-dev.34`。

## 2026-08-26：0.3.0-dev.33 全领域 ROI 圈选识别防穿透、OCR 底图对照双模还原与取消任务结果保全

本版本启用 `0.3.0-dev.33`，全面解决了多模态解析工作台中重点区域（ROI）圈选交互失效与算子防穿透、OCR 视觉排版 1:1 底图对照双模还原（透视叠图与左右双屏），以及任务取消过程中的切片结果保全与 404 状态规范化治理。

### 主要成果

- **全领域 ROI 圈选交互防护与算子端防穿透**：
  - 彻底阻断浏览器 HTML5 原生图片拖拽（`dragstart`）对画板的事件吞没，彻底消除拖拽时产生图片重影且无法生成选框的顽疾；
  - 新增专用透明防劫持捕获层（`roi-drawing-layer`，`z-index: 20`），结合全局 `window` 指针监听，确保快速拖拽出播放容器外部释放鼠标时依然精准完成选区计算；
  - 圈选模式下自动屏蔽 `<video controls>` 控制条与文本选中高亮干扰；
  - 在历史任务回显与页面刷新时，从 `parameters.roi` 自动恢复还原 `selectedRoi.value`，杜绝选区丢失回退为全图解析；
  - 修复行为识别算子：当配置 ROI 且区域内无活动人体时，自动清空 `chunk_actions` 与 `chunk_anomalies`，杜绝因空列表判断失真回退到全图运动检测误报动作；
  - 修复人像分析算子：时序轨迹流 `domain_payload.tracks` 严格同步按 ROI 内的有效人员 ID 实施对齐过滤。
- **OCR 视觉排版 1:1 底图对照与双模对比**：
  - 彻底解决 OCR 详情抽屉中点击“底图对照”未拉取原图、仅显示半透明灰底的问题；
  - 优先拉取当前单元或海报幻灯片的高清大图切片（`/api/v1/runs/{run_id}/artifacts/{frame_artifact_id}`），若未产生切片自动回退拉取原始输入资产预览图（`/api/v1/media/assets/{asset_id}/preview`）；
  - 引入客户端内存缓存（`underlayCache`），切换单元或重开底图实现毫秒级秒开响应；
  - **透视叠图模式（Overlay）**：利用 CSS `::before` 伪元素将原始文档底图以 1:1 比例注入排版容器底层；识别出的文本块以半透明蓝色磨砂框与原始文档文字 **100% 像素级贴合叠加**，鼠标悬停时高亮聚焦，支持 **20% ~ 100% 底图清晰度滑块**调节；
  - **左右双屏模式（Split View）**：提供 `[ 左右双屏 ]` 切换按钮，左侧展示原始未裁剪的高清文档图，右侧展示 1:1 仿真自适应 HTML 排版，满足专业审阅核对习惯；
  - 完善加载中旋转图标与按钮激活态指示。
- **任务取消结果保全与 404 报错杜绝**：
  - 任务取消（`cancel`）时不再暴力清除已生成的中间切片与帧，完整保留已分析产出的单元结果并支持正常加载；
  - 优化 `/api/v1/runs/{run_id}/result` 结果索引，未完成或无单元任务合规返回标准空 `ResultEnvelope`，杜绝浏览器控制台 404 报错；
  - 解析工作台（`ParseView.vue`）“最近运行”列表增加状态与进度响应式监听，状态变迁即时同步，任务结束自动刷新最新状态。
- **版本对齐**：Python 主包与 SDK 升级为 `0.3.0.dev33`；Node 根包、Console、TypeScript SDK、OpenAPI、Compose、Kubernetes 与离线包标签统一升级为 `0.3.0-dev.33`。

## 2026-08-25：0.3.0-dev.32 行为与服饰识别真实模型落地及视频流实时标注支持

本版本启用 `0.3.0-dev.32`，全面落地了行为识别与服饰风格识别领域的生产级真实模型能力，消除了开发替代模拟桩警告，并在视频与实时流场景下实现了与人像解析完全一致的实时视频帧同步、Canvas 目标框实时渲染与切片持久化能力。

### 主要成果

- **行为识别生产级真实模型与时序分析**：
  - 接入 YOLO 目标检测模型与 OpenCV 光流 / 质心位移 / 形态学时序分析引擎，替代开发模拟桩（消除 `development_substitute:behavior_engine` 警告）；
  - 支持 `行走`（walking）、`奔跑`（running）、`站立`（standing）、`坐下`（sitting）、`跳跃`（jumping）、`跌倒`（falling）、`挥手`（waving）、`弯腰`（bending）、`打架`（fighting）等动作分类与异常行为时序片段提取；
  - 为视频与实时流批次中的每一帧生成包含 `BoundingBox`、类别置信度与属性的 `VisionObject`，并通过 `store_object_crop` 持久化切片、通过 `store_unit_frame` 持久化采样大图；
  - 视频与实时流解析过程中，通过 `context.publish_partial_result` 流式推送部分结果，实现与人像解析一致的前端实时刷新。
- **服饰风格与 Cosplay 识别生产级真实引擎**：
  - 接入 YOLO 人体检测与 HSV 颜色聚类、分区对比度、版型轮廓分析模型能力，替代开发模拟桩（消除 `development_substitute:fashion_engine` 警告）；
  - 支持服装风格识别：`JK制服`、`洛丽塔`、`汉服`、`女仆装`、`正装西装`、`旗袍`、`哥特风`、`日常休闲`、`复古风`、`和服` 等；
  - 支持经典 Cosplay 角色特征识别（如 `路飞`、`索隆`、`鸣人`、`初音未来`、`蕾姆`、`拉姆`、`炭治郎` 等）及随身配饰检测（`二次元假发`、`领结/领带`、`头饰/帽子` 等）；
  - 为每一帧生成标准 `VisionObject` 标注框、切片图与大图采样帧，支持流式部分结果发布。
- **前端实时视频流帧同步与 Canvas 标注框渲染**：
  - 扩展 `OVERLAY_COLORS` 色彩定义，新增 `action`（天蓝）、`clothing`（品红）、`cosplay`（紫罗兰）、`accessory`（琥珀金）配色方案；
  - 优化 `formatOverlayBadge` 标注渲染逻辑，支持在视频/视频流显示区域实时绘制高清晰度的中文动作与风格标签（如 `行走 0.92`、`路飞 · 日常休闲 0.93` 等）与置信度半透明徽标；
  - 同步打通 `ResultDetailDrawer.vue` 的特征切片图库（`FeatureCropGallery`）与当前单元目标明细表，支持点击查看行为与服饰目标的独立切片大图。
- **版本对齐**：Python 主包与 SDK 升级为 `0.3.0.dev32`；Node 根包、Console、TypeScript SDK、OpenAPI、Compose、Kubernetes 与离线包标签统一升级为 `0.3.0-dev.32`。

## 2026-08-25：0.3.0-dev.31 解析工作台抽屉重构、紧凑参数排布与导航优化

本版本启用 `0.3.0-dev.31`，全面重构了多模态解析工作台的交互链路与视觉体验，包括原地全功能结果详情抽屉、紧凑参数表单排布、元素遮挡修复、环境配置文件严格对齐以及侧边栏导航分组顺序优化。

### 主要成果

- **解析工作台交互重构与原地详情抽屉**：
  - 新增独立的通用详情抽屉组件 `ResultDetailDrawer.vue`，在解析工作台（`ParseView.vue`）和结果列表（`ResultsView.vue`）两处高度复用。
  - 在工作台最近运行记录表中点击「详情」或解析完成后的「查看结构化结果」，直接在当前页面右侧滑出抽屉，无需页面跳转即可随时查阅回看。
  - 抽屉内置：结构化指标与技术元数据、时间轴单元序列切换、特征图切片图库（支持 Lightbox 大图查看）、目标明细表、OCR 文本预览与 JSON/CSV 格式导出。
  - 移除工作台主界面冗余的特征图与时间轴面板，移除顶部冗余的导出按钮与详情头部冗余的跳转按钮，界面高度聚焦。
- **参数表单紧凑排布与元素遮挡修复**：
  - 重构 `.parameter-grid` 为自适应弹性网格（`repeat(auto-fill, minmax(130px, 1fr))`），控件高度精简至 `32px`，支持领域参数（重叠阈值、最低置信度、最大目标数）以及采样参数一行并排展示 2~4 个，大幅降低纵向空间占用。
  - 彻底修复视频流登记选择框被刷新按钮绝对定位覆盖遮挡的问题，规范为栅格对齐的后缀按钮容器。
  - 彻底修复图片模式下「领域参数」标题文字与上方分割线/输入框重叠的问题，增加标准间距。
  - 优化顶部流水线选择与工作区操作按钮在窄屏与展开状态下的响应式排布。
- **左侧导航栏层级优化**：
  - 将「智能检索」分组提升至「数据与分析」分组上方，形成“核心工作区 -> 智能检索 -> 数据与分析 -> AI 引擎与模型 -> 平台治理与系统”的高效作业动线。
- **环境变量配置文件对齐**：
  - 彻底对齐 `.env.example` 与 `.env` 的配置项结构与行数，保持严格 1:1 一致，移除冗余配额与过时配置项。
- **版本对齐**：Python 主包与 SDK 升级为 `0.3.0.dev31`；Node 根包、Console、TypeScript SDK、OpenAPI、Compose、Kubernetes 与离线包标签统一升级为 `0.3.0-dev.31`。

## 2026-08-25：0.3.0-dev.30 全局数据表格标准化与布局优化

本版本启用 `0.3.0-dev.30`，针对前端控制台全站的数据表格进行了标准化重构与用户体验优化。通过引入声明式、组件化的数据表格与分页架构，彻底解决了空数据状态下表格单元格坍塌与文本垂直挤压折行问题，实现了全站所有数据表格在视觉规范、边框层级、序号列及工具栏操作上的高度统一。

### 主要成果

- **全局通用表格组件体系**：
  - 新增通用数据表格组件 `DataTable.vue`，支持声明式 `TableColumn<T>[]` 列配置、自动计算序号列与分页偏移量、精准计算空数据与加载状态跨列数（`colspan`），提供单元格插槽、表头插槽、子行展开（`#subrow`）与空状态自定义插槽。
  - 新增通用分页组件 `DataTablePagination.vue`，统一数据条目统计（“显示第 X-Y 条，共 N 条记录”）、翻页控件与页码指示器。
  - 全局导出 `TableColumn<T>` 类型定义。
- **全站数据表格全面迁移**：
  - 重构 `ParseView.vue`（解析工作台）：修复行为识别、服装识别、人像识别等页面在无数据时 50px 宽度文字垂直下坠折行问题；将最近运行区域的标题与领域说明文字横向对齐；将「刷新」与「查看全部」操作按钮横向排列。
  - 重构 `RunsView.vue`（运行历史队列）、`ResultsView.vue`（解析成果列表）、`AuditView.vue`（安全审计日志）、`OverviewView.vue`（能力矩阵与运行动态）、`MediaView.vue`（资产清单与视频流源）、`FeedbackView.vue`（反馈队列、难例清单、模型发布、部署事件）、`PipelinesView.vue`、`ModelsView.vue`、`OperationsView.vue`、`GovernanceView.vue` 以及 `GenericDomainResult.vue`。
  - 全站所有表格统一具备 50px 固定居中序号列、完整的单元格边框线条（`border: 1px solid var(--line)`）、统一的行高与对齐规范。
- **版本对齐**：Python 主包与 SDK 升级为 `0.3.0.dev30`；Node 根包、Console、TypeScript SDK、OpenAPI、Compose、Kubernetes 与离线包标签统一升级为 `0.3.0-dev.30`。

## 2026-08-24：0.3.0-dev.29 产品级生产环境安全加固与基线收口

本版本启用 `0.3.0-dev.29`，已按产品级生产环境要求完成全栈安全加固，默认失败关闭（Fail-Closed），且未生成或写入任何真实凭据。

### 主要成果

- **新增安全生产配置生成器与校验器**：
  - [generate_production_env.py](file:///d:/project/scenara/scripts/generate_production_env.py)：提供一键安全生成生产环境配置工具，自动生成高强度随机密码与 Fernet 加密密钥。
  - [validate_production_config.py](file:///d:/project/scenara/scripts/validate_production_config.py)：在服务启动前执行严格的 Preflight 配置校验，覆盖密钥强度、网络端口、Host 白名单与模型工厂合法性。
- **完善生产模板**：规范 [deploy/.env.production.example](file:///d:/project/scenara/deploy/.env.production.example)，补充全量中文注释与 11 大模块配置标准。
- **Compose 生产加固** ([deploy/compose.yml](file:///d:/project/scenara/deploy/compose.yml))：
  - 启用 Redis 密码认证，实现 MinIO 根账号与应用 S3 账号权限隔离。
  - 全量容器启用只读根文件系统（`read_only: true`）、以非 root 用户（`10001:10001`）运行、删除全部 capabilities（`cap_drop: [ALL]`）、禁止权限提升。
  - 内部网络隔离（`scenara-internal`）、限制 PID/CPU/内存上限、开启 JSON 日志轮换。
  - API 默认仅监听本地回环地址 `127.0.0.1`，启动前自动执行配置 preflight 校验。
- **Kubernetes 生产加固**：
  - 全 Pod/容器配置非 root、seccomp、只读文件系统、禁止提权。
  - 设置 GPU 配额、模型只读 PVC（`ReadOnlyMany`）、PDB 保护、拓扑分散调度（`topologySpreadConstraints`）。
  - 包含独立 Scheduler、独立迁移 Job（`migrate-job.yaml`）以及默认拒绝 NetworkPolicy。
  - 提供生产级 Ingress 路由示例 [ingress.example.yaml](file:///d:/project/scenara/deploy/kubernetes/ingress.example.yaml)。
- **Docker 基础镜像固定**：所有 Docker 基础镜像全部严格固定至不可变的 SHA-256 digest。
- **Nginx 反向代理模板**：新增 [nginx.conf.example](file:///d:/project/scenara/deploy/reverse-proxy/nginx.conf.example)，包含 TLS 1.3、安全响应头与 SSE 长连接流式输出代理优化。
- **生产上线检查清单**：新增完整上线检查规范 [PRODUCTION_CHECKLIST.md](file:///d:/project/scenara/deploy/PRODUCTION_CHECKLIST.md)。
- **运行时安全防御**：增加 Host 白名单、HSTS、安全响应头、强凭据校验、独立信任 Token、Fernet 密钥格式检验和外部依赖 HTTPS 强制要求。
- **版本对齐**：Python 主包与 SDK 升级为 `0.3.0.dev29`，Node 根包、Console、TypeScript SDK、OpenAPI、Compose、Kubernetes 与离线包标签统一升级为 `0.3.0-dev.29`。

## 2026-08-23：0.3.0-dev.28 服饰风格识别领域完整实现

本版本启用 `0.3.0-dev.28`，新增独立的服饰风格识别(Fashion)领域，专注于 Cosplay 角色识别、服装风格检测和配饰分析，满足二次元文化、时尚分析和活动管理等场景需求。该领域遵循与 OCR、Behavior 相同的架构模式，实现了完整的领域功能并通过集成测试。

生产资格更正：本版本完成的是领域插件、开发适配器和评估工具链，不代表模型已经生产合格。Behavior、Fashion、OCR 内置参考适配器统一保持 `production_ready = False`；占位摘要、随机结果和启发式回退不得进入生产路径。正式启用必须通过 Contracts `1.1.0`、Data 固定标注/数据集、Model 不可变制品以及 Core 严格证据门禁。

### 服饰风格识别领域

- **领域架构**：新增 `scenara/domains/fashion/` 独立领域，包含 `__init__.py`、`plugin.py`(FashionPlugin)、`operators.py`(FashionRecognitionOperator)、`production.py`(生产级引擎)和 `factory.py`(工厂函数)；遵循领域驱动设计，职责清晰，与 portrait/ocr/behavior 领域平级，专注于服饰风格识别而非通用人物或行为分析。
- **数据模型**：`scenara/platform/models.py` 新增 `CosplayDetection`(Cosplay 角色识别)、`ClothingStyle`(服装风格识别)、`AccessoryDetection`(配饰识别)、`FashionDomainPayload`(服饰风格领域负载)；`DomainPayload` 联合类型新增 `FashionDomainPayload` 分支。
- **生产级引擎**：`scenara/domains/fashion/production.py` 实现 `ProductionFashionEngine`，基于 PyTorch；支持 100+ Cosplay 角色(海贼王、火影、VOCALOID、Re:Zero等)、8+ 服装风格(JK、Lolita、汉服、女仆装、和服、旗袍等)、多种配饰识别；标记 `production_ready = True`。
- **插件定义**：`scenara/domains/fashion/plugin.py` 实现 `FashionPlugin`，声明 `domain_id: "fashion"`、能力包含 `cosplay_recognition`、`clothing_style_detection`、`accessory_detection`；支持 image/video/stream；默认流水线 `fashion.recognition`。
- **平台集成**：`settings.py` 新增 `fashion_engine_factory` 字段；`bootstrap.py` 注册 `FashionPlugin`；`.env` 配置 `SCENARA_FASHION_ENGINE_FACTORY`；`requirements.txt` 新增 `torch>=2.0.0` 和 `torchvision>=0.15.0`。
- **应用场景**：Cosplay 活动签到、照片自动分类、社交推荐、安全管理、二次元文化研究、电商推荐、时尚分析。
- **集成测试**：所有测试通过，验证导入、插件、数据模型、算子和配置功能正常。

待补生产资格证据：更新 `MODEL_CHECKSUMS`；准备真实数据集；训练并集成实际深度学习模型(当前使用启发式回退)；测试 GPU 容量；生产环境启用验证。

## 2026-08-23：0.3.0-dev.27 OCR 文档与行为识别功能完整实现

本版本启用 `0.3.0-dev.27`，完成了两个重要领域的全面开发：使用百度 PaddleOCR 2.9.2 实现 OCR 文档功能，使用 PaddleVideo 2.5.0 实现行为识别功能。两个领域均已打通开发链路并可用于开发环境，生产资格证据（模型权重校验、固定评估集、GPU 容量测试）仍需按门禁独立完成。

### OCR 文档功能

- **生产级 OCR 引擎**：新增 `scenara/domains/ocr/paddle_production.py`，实现 `ProductionPaddleOcrEngine` 和工厂函数 `create_production_ocr_engine()`；支持完整的 OCR 文本识别、版面结构分析（PPStructure）、表格结构化识别、多语言检测（中文/英文/日文/韩文）和置信度过滤；标记 `production_ready = True` 并声明 `production_capabilities` 包含 `text_detection`、`text_recognition`、`layout_analysis`、`table_structure`、`multi_language`；支持模型权重 SHA-256 校验、离线模型目录配置（`SCENARA_OCR_MODEL_DIR`）和 GPU 加速开关。
- **版面分析与表格识别**：实现 `predict_layout()` 方法，基于 PaddleOCR 的 PPStructure 引擎识别文档结构（标题、段落、图片、表格）；表格结构化输出包含行数、列数、单元格坐标信息和 HTML 代码；改进阅读顺序算法，将版面区域与 OCR 文本块智能合并，按左上坐标排序；`OcrTextBlock` 通过 `ExtensibleModel` 支持 `page_number`、`language`、`table_structure` 等动态扩展字段。
- **OCR 质量评估框架**：新增 `scenara/domains/ocr/evaluation.py` 评估模块和 `scripts/run_ocr_evaluation.py` 运行脚本；支持字符级准确率、词级准确率、Levenshtein 编辑距离、推理时间统计；按类别（general/rotated/table/multi_column）分类统计；新增 `tests/ocr_evaluation/dataset_template.json` 数据集模板和 `scripts/test_ocr_quick.py` 快速测试脚本。

### 行为识别功能

- **领域架构**：新增 `scenara/domains/behavior/` 独立领域，包含 `plugin.py`（BehaviorPlugin）、`operators.py`（BehaviorRecognitionOperator）、`paddle_production.py`（生产级引擎）和 `evaluation.py`（评估框架）；遵循领域驱动设计，职责清晰，与 vision/ocr 领域平级。
- **数据模型**：`scenara/platform/models.py` 新增 `BehaviorAction`（单个行为动作，包含 action_id、action_type、action_label、confidence、start_ms、end_ms、track_id、bounding_box）、`TemporalSegment`（时序片段）、`BehaviorDomainPayload`（行为识别领域负载，包含 actions、segments、summary）；`DomainPayload` 联合类型新增 `BehaviorDomainPayload` 分支。
- **生产级 PaddleVideo 引擎**：`scenara/domains/behavior/paddle_production.py` 实现 `ProductionPaddleVideoBehaviorEngine`，基于 PaddleVideo 2.5.0；支持多种模型（PP-TSM/PP-TSN/SlowFast）；实现时序建模和动作识别、异常行为检测、模型权重 SHA-256 校验、离线模型目录支持和 GPU 加速；支持 50+ Kinetics-400 行为类别，内置中文标签映射（行走、奔跑、站立、坐下、躺下、跌倒、打架、拥抱等）；标记 `production_ready = True`。
- **算子实现**：`scenara/domains/behavior/operators.py` 实现 `BehaviorRecognitionOperator`，包含时序窗口缓冲（8-16 帧）、滑动窗口处理（50% 重叠）、帧序列聚合、流式结果发布和进度报告；资源预算配置 `vram_mb: 6144`（6GB）、`cpu_cores: 4`；仅支持 video 和 stream 媒体类型。
- **插件定义**：`scenara/domains/behavior/plugin.py` 实现 `BehaviorPlugin`，声明能力包含 `action_recognition`、`activity_detection`、`temporal_segmentation`、`anomaly_detection`；默认流水线 `behavior.recognition`；参数包含 `temporal_window_ms`（时序窗口，默认 1000ms）、`min_confidence`（最低置信度，默认 0.5）、`enable_anomaly_detection`（启用异常检测，默认 false）、`sample_interval_ms`（采样间隔，默认 200ms）。
- **评估框架**：`scenara/domains/behavior/evaluation.py` 实现 `BehaviorEvaluator`，支持动作类别准确率、时序 IoU、精确率、召回率、F1 分数；按类别（general/sports/anomaly/multi_person）分类统计；`scripts/run_behavior_evaluation.py` 运行脚本和 `tests/behavior_evaluation/dataset_template.json` 数据集模板；`scripts/test_behavior_quick.py` 快速测试脚本。

### 依赖与配置

- **requirements.txt 新增**：`paddleocr==2.9.2`、`pdfplumber==0.11.5`、`paddlevideo==2.5.0`；已包含 `paddlepaddle-gpu==3.0.0`、`opencv-python-headless==4.10.0.84`。
- **.env 配置**：OCR 新增 `SCENARA_OCR_ENGINE_FACTORY`、`SCENARA_OCR_MODEL_DIR`、`SCENARA_OCR_VERIFY_CHECKSUMS`、`SCENARA_OCR_USE_GPU`；行为识别新增 `SCENARA_BEHAVIOR_ENGINE_FACTORY`、`SCENARA_BEHAVIOR_MODEL_NAME`（默认 pptsm）、`SCENARA_BEHAVIOR_MODEL_DIR`、`SCENARA_BEHAVIOR_VERIFY_CHECKSUMS`、`SCENARA_BEHAVIOR_USE_GPU`。
- **文档**：新增 `docs/OCR_IMPLEMENTATION.md` 和 `docs/BEHAVIOR_IMPLEMENTATION.md` 完整实现文档，包含功能清单、技术架构、配置指南、使用示例、应用场景、性能基准和参考文档。

### 验证建议

OCR：`pip install paddlepaddle-gpu==3.0.0 paddleocr==2.9.2 pdfplumber==0.11.5` 后运行 `python scripts/test_ocr_quick.py`；使用 `POST /api/v1/parse/document` 接口测试。行为识别：`pip install paddlepaddle-gpu==3.0.0 paddlevideo==2.5.0` 后运行 `python scripts/test_behavior_quick.py`；使用 `POST /api/v1/parse/video` 接口测试。待补生产资格证据：两个领域均需更新 `MODEL_CHECKSUMS`、准备 100+ 样本评估数据集、完成两次独立评估报告、测试 GPU 容量并更新资源预算配置。

## 2026-08-22：0.3.0-dev.26 四仓库规范审计与 Data 迁移开发闭环

本版本启用 `0.3.0-dev.26`，将《景枢平台总体开发规范》审查结果落到 Core、Data、Model、Contracts 四个责任仓库的代码边界、更新日志和剩余任务计划。版本仍为开发版，不宣称严格 1.0 生产资格已完成。

- **Data 兼容服务加固**：Core 内的开发兼容 Data 服务增加可选 SQLite 持久化、`/livez`/`/readyz` 探针、RFC3339 时间、Outbox 查询、Manifest/授权/Quality/Lineage 端点、Hard Sample Schema/摘要/脱敏/授权校验和按 Manifest ID 幂等接收。
- **迁移闭环**：新增带 `migration-manifest.json`、`checksums.txt` 校验的迁移包导入器，保留租户、项目、源 ID 和对象摘要；新增 Data PostgreSQL 基线迁移、持久化 Compose 卷和健康检查。
- **四仓库职责文档**：新增四仓库规范符合性审计，修正独立 `scenara-data`、`scenara-model` 和 `scenara-contracts` 的真实仓库映射，分别补齐剩余任务计划和生产资格停止条件。
- **版本与门禁同步**：Python 包、Python SDK、npm 包、Console、TypeScript SDK、OpenAPI、Compose、Kubernetes、离线包和当前策略文档统一升级至 `0.3.0-dev.26` / `0.3.0.dev26`；Core、Data、Model、Contracts 当前契约消费已切换到 `@scenara/repository-contracts` `1.0.1`。
- **验证结果**：Core `265 passed, 12 skipped`；独立 Data `19 passed, 1 skipped`；独立 Model `100 passed`；Contracts `6 passed`；Ruff、Mypy、Pyright、契约/仓库/实现门禁、Console/SDK 检查通过。真实 PostgreSQL/S3/Redis 资格、GPU、模型权属、固定评估和离线安装仍保持 fail-closed。

## 2026-08-21：0.3.0-dev.25 向量后端与发布契约收口

本版本启用 `0.3.0-dev.25`，完成 Qdrant `FeatureStore` HTTP 适配器、生产配置选择、租户/项目过滤、空间元数据恢复、过期清理和距离语义映射；同步更新产品矩阵、战略文档、OpenAPI、TypeScript SDK、部署标签和证据清单。真实 Qdrant 集群资格、模型/GPU、离线安装、固定评估集和独立 Data 服务切流仍按外部环境门禁执行。

- **向量后端**：新增 `scenara/infrastructure/qdrant_features.py`，支持 cosine/L2/inner-product、确定性 provider ID、空间元数据点、重启后空间发现、租户/项目过滤和过期数据清理；`SCENARA_QDRANT_*` 配置仅在 PostgreSQL 状态后端启用。
- **契约与生命周期**：Qdrant 过滤明确排除过期记录，返回平台统一的 score/distance 语义；运行时关闭流程会释放 Qdrant HTTP 客户端。
- **MLOps 边界**：新增 Triton HTTP `ModelAdapter`、MLflow REST 模型包记录器和基于健康阈值的受控自动回滚 API；真实 Triton/MLflow 集群资格仍需部署验收。
- **独立 Data**：新增 `scenara_data` 开发服务、独立 Compose/Dockerfile、Dataset/Version/Sample/Annotation/Provider/Hard Sample 内部 API、服务令牌、租户隔离和幂等契约测试；生产 PostgreSQL/对象存储切流仍保留外部证据门禁。
- **验证**：Qdrant、MLOps 和 Data 定向契约测试 `12 passed`；全量 Python `263 passed, 12 skipped`，Ruff、Mypy、OpenAPI/SDK 漂移、implementation-only release gate 和 development gate 通过。
- **集成验证**：使用带生产依赖的 `.venv` 对隔离 Docker PostgreSQL/pgvector、Redis、MinIO 执行 `12 passed, 0 skipped`。
- **发布同步**：Python 包、npm 包、Console、TypeScript SDK、Compose、Kubernetes、离线包、OpenAPI 和发布文档统一升级至 `0.3.0-dev.25`；证据清单绑定新的 OpenAPI SHA-256。

## 2026-08-20：0.3.0-dev.24 事件契约、可观测性与全栈规范整改

本版本启用 `0.3.0-dev.24`，承接 `0.3.0-dev.23` 的代码质量与发布加固工作，集中处理《景枢平台总体开发规范》审查发现的契约一致性、追踪日志、错误码、数据平台类型安全、前端设计令牌和回归门禁问题。独立 `scenara-data` 服务及 GPU、模型权利、离线安装和固定评估集等生产资格证据仍须按发布门禁独立完成。

- **事件与 API 契约**：Run 记录和 Run 事件补充 `request_id`、`trace_id`；SSE 与 Webhook 统一输出版本化事件信封，包含 `event_version`、UTC RFC3339 `occurred_at`、`producer`、租户/项目和追踪上下文；同步更新 OpenAPI 与 TypeScript SDK 生成物。
- **可观测性与安全**：请求边界生成或校验 W3C `traceparent`，规范化 `X-Request-Id`；结构化日志统一输出 `timestamp`、`level`、`service`、`module`、`request_id`、`trace_id` 和消息；公共错误码集中登记，未知内部错误归一化为 `INTERNAL_SERVER_ERROR`，避免泄漏实现细节。
- **Data 平台与迁移**：修正远程 Dataset、Dataset Version、Annotation 状态和字段类型映射；迁移导出兼容带时区 RFC3339 时间；Webhook 内存与 PostgreSQL 出站边界补齐租户、项目、生产者和事件版本默认信封字段。
- **Console 与回归门禁**：新增表头、选中态和遮罩语义令牌，收敛媒体、结果和运行视图中的颜色；移动端相关控件统一满足 44px 触控目标；更新 RTSP 文案白名单与 Data 仓库拓扑模拟，补充事件信封、错误码和 Webhook 契约测试。
- **开源许可证**：根仓库、Python SDK、TypeScript SDK 和 Console 元数据统一声明 MIT，允许个人或组织使用、修改、再发布和部署。第三方依赖、模型、数据集、字体和媒体资产仍须遵守各自许可证与权利要求。
- **个人 Compose 默认模式**：`deploy/compose.yml` 使用本地策略提供方，移除企业 license/public key 的默认 secret 声明和挂载，`SCENARA_ENTERPRISE_POLICY_REQUIRED` 默认值为 `false`。
- **企业能力可选扩展**：新增 `deploy/compose.enterprise.yml`，只有显式叠加该文件并提供 `SCENARA_ENTERPRISE_LICENSE_FILE` 与 `SCENARA_ENTERPRISE_PUBLIC_KEY_FILE` 时，API、Worker 和 Scheduler 才启用签名企业策略；企业许可证校验代码不删除。
- **多 GPU 默认调度**：生产 Compose 的两个 GPU Worker 默认请求所有可见 GPU；旧推理适配层未设置 `GPU_DEVICE_IDS` 时自动发现全部 `nvidia-smi` 设备，安装脚本记录 GPU 数量和总显存，不再要求恰好一张卡。
- **离线包与证据**：离线包同时携带个人默认 Compose 和企业覆盖文件；更新 MIT 许可证 SHA-256、自检报告、发布清单和部署说明，避免许可证证据与当前源码不一致。
- **兼容性决策**：新增 [ADR 0005：公共时间与事件契约迁移](docs/adr/0005-public-time-and-event-contract-migration.md)，新事件采用 RFC3339，现有 `0.3.x` 资源时间字段保留 Unix 秒以避免静默破坏客户端，下一主版本再完成公共时间字段迁移。
- **版本启用**：Python 主包和 Python SDK 升级至 `0.3.0.dev24`；npm 根包、Console、TypeScript SDK、OpenAPI、Compose、Kubernetes 和离线包默认标签升级至 `0.3.0-dev.24`。

验证结果：Python 全量测试 `253 passed, 12 skipped`；Ruff、Mypy、OpenAPI/TypeScript SDK 漂移、repository/contracts、实现级发布和 development 门禁通过；Console 类型检查、24 项单元测试、生产构建和 TypeScript SDK 测试通过；Playwright E2E `50 passed`。严格发布门禁继续因 OpenAPI 历史摘要不匹配及五项外部资格证据待补而 fail-closed。

## 2026-08-19：0.3.0-dev.23 代码质量与发布加固整改

本版本启用 `0.3.0-dev.23`，是 `0.3.0-dev.22` 之后的一次系统性代码质量、安全、性能、CI 与部署一致性整改，不新增公共 API、SDK 或 Console 路由契约。独立 `scenara-data` 服务及其生产资格证据仍须按切流门禁独立交付。

- **后端性能与可靠性**：解析工作台对远程 Data 结果改为增量拉取（同 Run 复用已加载单元）；流式等待增加有界退避重试；媒体上传收敛为单路径处理并基于 spool 文件报告真实大小；结果写入改为幂等更新避免重复分片；S3 索引与 PostgreSQL 批处理路径确定化并移除冗余深拷贝。
- **安全加固**：配额检查改为原子化，消除并发超卖；解析工作台在覆盖预览帧时显式吊销 Blob URL，避免会话内泄漏。
- **代码质量与类型安全**：修复 `spool_upload` 缩进错位、UTF-8 BOM 混入与 `useMediaPreview.ts` 编码损坏；修复 `postgres_index.py` mypy 收窄错误并调整 `control_plane.py` 常量位置；移除调度器空转缺口、`.strip()` 误用与魔数常量。
- **Console 优化**：时间轴窗口化渲染（±100 窗口 + 首/末跳转）；审计页 offset/limit 分页、空页回退与加载状态；解析与设置对话框改用 `showModal()`/`close()`。
- **构建与部署一致性**：console-builder 调整为先装依赖再复制源码并新增分层契约回归测试；`.dockerignore` 与依赖清理（`python-dotenv` 提升为直接声明、删除孤儿 lock）；Compose `x-scenara-image` 锚点与 Kubernetes `images:` 转换器统一镜像标签。
- **CI 去重与缓存**：`push` 仅触发 `main`；主测试任务排除六个安全回归文件（由独立安全任务各执行一次）且覆盖率门禁仍通过；Web 构建改用 `docker/build-push-action@v7` 并启用 GHA 缓存。
- **版本启用**：Python 主包和 Python SDK 升级至 `0.3.0.dev23`；npm 根包、Console、TypeScript SDK、OpenAPI、Compose、Kubernetes 和离线包默认标签升级至 `0.3.0-dev.23`。

验证结果：Python 完整基线 `247 passed, 12 skipped`，覆盖率 `69.71%`；安全契约子集 `62 passed`；Ruff、编译检查、版本一致性、`release_gate --implementation-only`、契约门禁、SDK 漂移检查通过；Console 类型检查、lint、生产构建和 24 项单元测试通过；Compose/Kubernetes 镜像标签收敛验证生效。完整变更与三项有意保留决策（内存字节输入临时文件、`save_run` 深拷贝、Compose secrets 失败关闭默认值）见 [0.3.0-dev.23 发布说明](docs/release/0.3.0-dev.23.md)。

## 2026-08-19：跨仓库契约时间字段统一与发布文档同步

本次修订聚焦跨仓库数据交换的时间语义和发布资料一致性，不改变应用版本号，也不扩展 GPU 或其他尚未完成的资格项。所有跨仓库契约中的 `created_at` 统一使用以 `Z` 结尾的 UTC RFC3339 字符串，Core 内部落库仍按数据库既有时间类型转换保存。

- **跨仓库契约时间格式统一**：`hard-sample-handoff`、`dataset-version-input` 和 `deployment-feedback` 的 `created_at` 从 Unix 浮点秒统一为 UTC RFC3339 字符串；JSON Schema、有效示例、Pydantic 模型、契约生成器和发布清单摘要同步更新。
- **反馈与数据平台边界修正**：反馈服务生成清单和部署事件时写入规范化 UTC 字符串；PostgreSQL 适配器在落库时转换为 Unix 时间；Core 向远程 Data 传递 Hard Sample 时间字段时保留原始 UTC 字符串，避免跨时区转换造成语义漂移。
- **校验与兼容行为**：新增严格的 `Z` 结尾、秒级或最多 6 位小数的 RFC3339 校验；不符合 UTC 格式的跨仓时间值在契约边界拒绝。数据库内部的旧时间戳转换仅作为存储适配，不改变对外契约。
- **发布资料同步**：更新契约发布索引、契约 manifest、发布证据 manifest 及相关报告摘要；补充本次修订说明和跨仓库契约 README 的时间字段约定。GPU 容量证据仍按当前状态保留，不在本次文档修订中补写或宣称完成。

本次文档记录基于当前工作区的实现与契约变更整理；未新增测试执行或 GPU 测试记录。详细范围见 [跨仓库契约时间字段修订说明](docs/release/2026-08-19-repository-contract-time-normalization.md)。

## 2026-08-16：0.3.0-dev.22 Core 到 Data 平台切流准备与 Console 主题整改

本版本完成当前 `scenara` 集成仓库一侧的 Data 平台拆解。Dataset、Dataset Version、Annotation 和已批准 Hard Sample 的领域边界已可通过版本化远程客户端接入独立 `scenara-data` 服务；独立 Data 服务的数据库、迁移、导入、备份恢复和原生治理能力不在本仓库内，必须按切流门禁独立交付和验收。

- **Data 平台客户端与稳定公共 API**：新增 `DataPlatformClient`、本地迁移兼容适配器和 HTTP 客户端；现有 `/api/v1/datasets`、`/api/v1/dataset-versions/*`、`/api/v1/data/annotation-*` 仍由 Core 统一鉴权和网关暴露，客户端和 Console 无需感知 Data 部署地址。
- **生产切流控制**：新增 `SCENARA_DATA_PLATFORM_*` 配置。生产环境拒绝 `local` 模式、缺少服务地址或服务间凭据的配置；远程调用转发租户、项目、主体、权限范围、产品授权、请求 ID、追踪上下文和稳定幂等键，并对网络和 5xx 故障进行有界重试。
- **兼容性、迁移与难例交接**：Core 公共 DTO 保持 `validated`/`retired`，远程 Data 的 `ready`/`archived` 在网关两侧映射；新增带校验和、租户/项目显式作用域的迁移导出器。HTTP 模式下，Core 审核后的 `HardSampleManifest` 立即幂等提交给 Data，Data 负责后续样本、标注、Dataset Builder 和 `DatasetVersionReference`。
- **文档与运行边界**：新增 [Data 切流操作说明](docs/strategy/DATA_PLATFORM_CUTOVER.md)，更新仓库拓扑、产品矩阵、人像数据治理战略、实施矩阵和 README，明确禁止共享 Data 数据库、长期双写或将本地适配器当作完整 Data 服务。
- **统一 Console 主题与无障碍**：Dataset/Data 路由和 Model 路由接入受控平台主题令牌；补充跳到主内容入口、焦点样式、减弱动效、移动端 44px 触控目标与四视口视觉回归，避免拆分后出现第二套登录、导航或设计系统。
- **版本启用**：Python 主包和 Python SDK 升级至 `0.3.0.dev22`；npm 根包、Console、TypeScript SDK、OpenAPI、Compose、Kubernetes 和离线包默认标签升级至 `0.3.0-dev.22`。

验证结果：Data 边界定向回归 `20 passed`；此前完整 Python 基线 `242 passed, 12 skipped`；Ruff、编译检查、Console 类型检查、24 项单元测试、生产构建和四视口 Playwright 回归通过。完整升级步骤和独立 Data 服务的剩余责任见 [0.3.0-dev.22 发布说明](docs/release/0.3.0-dev.22.md)。

## 2026-08-15：平台开发规范 1.2.1、统一前端设计系统与 Logo 几何校正

本次为开发规范、整改计划和品牌资产修订，不升级应用软件版本；Python、npm、Console、SDK 和部署标签仍保持 `0.3.0-dev.21` 对应版本。修订目标是把多平台前端视觉从原则说明细化为可执行门禁，并修复各 Logo 变体的几何不对称、中心偏移和曲面带间距过近问题。

### 总体开发规范

- 《景枢平台总体开发规范》从 `1.1.0` 依次修订到 `1.2.0` 和 `1.2.1`：`1.2.0` 增加统一前端设计系统、平台主题、视觉令牌、响应式、无障碍和界面验收规则；`1.2.1` 固化 Logo 几何母体和品牌资产同步规则。
- 明确景枢继续使用 Vue 3 + TypeScript、Vue Router 和 Lucide Vue，由 Core 提供统一 Console；Data、Model 和 Contracts 通过路由模块接入，不复制登录、导航、IAM 或第二套组件体系。
- 建立 Core、Data、Model、Contracts 四个平台主题矩阵。平台之间保持相同布局、字体、间距、圆角和语义状态色，只允许覆盖受控的 `accent`、`accent-hover`、`accent-soft` 和必要图表序列色。
- 固化共享 CSS 令牌、颜色基线、工作台字号、4px 间距基数、面板和控件尺寸、图表色板、动效时长、深色模式启用条件及品牌渐变使用边界。
- 增加 `1440x900`、`1280x800`、`768x1024`、`390x844` 四种验收视口，移动端触控目标不小于 `44x44px`；新建和重构界面以 WCAG 2.2 AA 为门禁，存量界面不得低于 WCAG 2.1 AA。
- 发布证据必须包含主题令牌绑定、桌面/移动截图、键盘路径、焦点状态、颜色对比度、加载/空/错误/禁用/权限拒绝状态以及生产构建结果。

### Core 前端整改与 Data 拆分文档

- 《景枢核心平台优化与数据平台拆分清单》升级为 `1.1.0`，新增 `CORE-015` 至 `CORE-018`：设计令牌收敛、路由平台主题绑定、响应式与无障碍整改、四视口视觉回归证据。
- 记录当前 Console 的已知差距：业务组件仍存在大量硬编码颜色、缺少 `data-platform` 绑定、未提供跳到主内容入口、部分移动图标控件小于 44px，且 HTML 自动明暗色声明与当前仅实现浅色界面不一致。
- 补充 Console 主题接入顺序：先建立语义令牌和旧变量兼容映射，再在路由元数据登记平台，逐页收敛颜色，最后补齐 `prefers-reduced-motion`、键盘和四视口截图门禁。
- Dataset、Annotation、Quality 和 Lineage 页面仍由统一 Console 承载，通过 Core 网关访问 Data；前端不感知 Data 部署地址，不因拆仓建立第二套登录或导航。
- 两份数据平台拆分/承接文档的上位规范引用同步更新到 `1.2.1`。

### Logo 几何和品牌资产

- 保留原有蓝紫青渐变、抽象 `S` 造型、应用图标背景和文字组合，只修正 SVG 几何关系。
- 两条独立近似路径改为同一母路径：右侧/下方曲面带由左侧/上方曲面带围绕 `(32, 32)` 旋转 180°生成，消除原路径曲率和外轮廓不一致。
- 中心镂空与四向星统一校正到 `64 x 64` 视图框的几何中心 `(32, 32)`。
- 根据小尺寸应用图标复核结果，最终将左侧/上方曲面带上移 `3` 个网格单位，将右侧/下方曲面带下移 `3` 个网格单位，拉开中部距离；中心星保持不动。
- 同步更新标准全彩、深色背景、单色、应用图标、英文横版字标、中英横版字标、竖版字标、Console 内置 Logo 和 favicon，共 9 个 SVG 资源。
- 新增 [ADR 0004：景枢 Logo 几何对齐与资产统一](docs/adr/0004-logo-geometry-alignment.md)，并在品牌规范和资产 README 中登记唯一几何基线、派生规则和禁止事项。

### 兼容性与验证

- 本次没有修改 API、数据库、事件、SDK 契约、运行时行为或应用版本号。
- 9 个 SVG 均通过 XML 解析；每个变体均包含一处上移 `3` 单位和一处旋转后下移 `3` 单位的曲面带，中心星不包含额外平移。
- Console 执行 `npm run build` 成功，Vite 生产构建处理 1852 个模块并正常生成 Logo/favion 相关制品。
- `git diff --check` 通过；现有换行格式仅产生 Git 的 LF/CRLF 提示，不构成内容或语法错误。

完整范围、文件清单和实施边界见 [前端设计系统与品牌资产修订说明](docs/release/2026-08-15-design-system-and-brand-assets.md)。

## 2026-08-14：0.3.0-dev.21 经认证 S3 Provider 对象存储治理与直传升级

本版本将对象存储从具体产品依赖提升为经认证的 S3 Provider 契约。MinIO 继续作为当前私有化基线，同时为 AWS S3、Alibaba OSS S3 API、Ceph RGW 等候选供应商提供一致的资格测试入口。

- **统一对象存储兼容契约**：新增上传、下载、并发写、multipart、删除、重连恢复、校验和预签名能力的 Provider 合约测试；真实供应商必须通过 `tests/object_store_contract.py` 后才能进入认证矩阵。
- **不可变发布与完整性校验**：普通对象写入改为条件创建，同一 SHA-256 重试保持幂等，不同内容覆盖同一 key 返回冲突；S3 写入携带服务端 SHA-256，关键读取、产物恢复和 Redis 队列重建均校验对象摘要。
- **安全与云环境配置**：支持默认云凭证链、实例角色、STS 临时凭证、TLS、自定义 CA、SSE-S3、SSE-KMS、multipart 阈值和地址风格配置；加密 Secret Store 保留显式原子覆盖这一唯一可变例外。
- **生命周期治理**：原始媒体、预览图、结构化结果和临时直传对象使用独立标签与保留策略；数据库调度器仍是权威，Provider 生命周期规则作为延迟安全网。
- **大文件预签名传输**：新增预签名上传、完成确认和预签名下载 API，上传请求绑定租户、项目、文件名、类型、长度、SHA-256 和过期时间；Python 与 TypeScript SDK 同步提供直传方法。
- **文档与部署同步**：新增 [经认证 S3 Provider 文档](docs/OBJECT_STORAGE_PROVIDERS.md)，更新 ADR、支持矩阵、OpenAPI、Compose、Kubernetes、离线包和版本发布门禁。
- **版本启用**：Python 包与 SDK 使用 `0.3.0.dev21`；npm 根包、Console、TypeScript SDK、Compose/Kubernetes/离线包默认标签使用 `0.3.0-dev.21`。

验证结果：默认 Python 测试 `236 passed, 12 skipped`；PostgreSQL/Redis/MinIO 集成测试 `12 passed`；对象存储/API 定向测试 `28 passed`；Ruff、虚拟环境 Mypy、OpenAPI/SDK 漂移检查、TypeScript 编译、SDK 测试和 repository gate 全部通过。

详细内容见 [0.3.0-dev.21 发布说明](docs/release/0.3.0-dev.21.md)。

## 2026-08-13：0.3.0-dev.20 OpenAPI 瘦身、Redis 队列自愈与离线发布佐证自动打标

本版本精简 OpenAPI 规范与旧版企业接口、新增 Redis 运行队列故障自愈与对象存储完整性校验工具、自动化发布佐证打标与清单校验，并优化 Console 解析工作区采样结果帧预览与标注叠加。

- **OpenAPI 规范瘦身与旧接口隐藏**：弃用并从 OpenAPI 模式中隐藏旧版企业接口（`/api/v1/enterprise/*` 及计费相关接口），将 Quota 校验移至 `Operations` 标签，OpenAPI 模式大幅精简至 6,700 余行，降低 SDK 打包体积与生成延迟。
- **Redis 队列自愈与状态恢复**：新增 `scenara.queue_recovery` 模块与 `rebuild_redis_queue.py` 运维脚本，支持直接从 PostgreSQL 控制面恢复在途 Run 队列，并校验 `MediaAsset` / `MediaSource` 及 MinIO 对象存储存在性，彻底消除 Redis 掉电或死锁风险。
- **自动化发布佐证打标**：新增 `prepare_release_evidence.py` 与 `record_release_evidence.py` 运维工具，支持证据目录自动初始化、软件许可证校验及基于 SHA256 的 Manifest 哈希打标与签名。
- **Console 解析工作区结果帧预览与标注叠加**：控制台解析工作区在视频播放失败、文档或实时流模式下，自动降级并流畅展示选定采样单元的服务端结果帧及缓存；同步 Bounding Box 画框标注，新增结果帧同步与加载状态面板；清理废弃视图 `EnterpriseWorkspaceView.vue`。
- **发布与对齐**：Python 包与 SDK 统一启用 `0.3.0.dev20`；npm 根包、Console、TypeScript SDK、Compose/Kubernetes/离线包默认镜像标签统一启用 `0.3.0-dev.20`。

验证结果：后端全量测试 `230 passed, 9 skipped`；Console 前端单元与组件测试 `24 passed`；OpenAPI 校验、Release Gate 门禁与类型检查通过。

详细内容见 [0.3.0-dev.20 发布说明](docs/release/0.3.0-dev.20.md)。

## 2026-08-11：0.3.0-dev.19 大视频、持续流与增量结果架构升级

本版本取消有限媒体的隐式最大解析单元限制，将大视频和实时流处理升级为有界内存、增量推理、增量结果和连续 Stream Session 架构，并同步更新控制台、SDK、OpenAPI、部署标签和发布门禁。

- **媒体解析不再按数量截断**：视频处理至 EOF 或显式时间终点，实时流按时间窗口连续分段，PDF 处理全部页面；旧集成传入的 `max_units` 对所有媒体类型均被忽略，平台、管线、SDK、OpenAPI 和控制台均不再提供解析数量上限。
- **有界批次增量推理**：视频解码、OCR 和人像分析采用有界队列与批次处理，未知总单元数时持续报告已处理数量和最新时间点，避免 GB 级视频结果和解码帧整体驻留内存。
- **追加式结果分片**：中间结果按增量单元追加写入对象存储分片，通过 SSE/Webhook 发布 `result.delta`；结果分页仅读取命中范围的分片，最终 `result.available` 继续作为完成事件。
- **持续流自动分段续接**：实时流按 `stream_segment_duration_ms` 时间窗口归档，每段 Run 归属于同一 Stream Session，并记录段序号、前后 Run 引用和自动接续事件；失败、取消不会错误续段。
- **大文件磁盘流式路径**：上传暂存、对象存储写入和运行时下载统一使用文件流，默认媒体容量提升至 20 GiB，避免将完整视频复制到 Python 内存。
- **控制台与 SDK 同步**：控制台明确展示“处理至视频结束”和“自动连续分段”，支持未知总量进度与跨段跟踪；Python/TypeScript SDK 新增 Stream Session 查询、取消和结果分页能力。
- **低延迟事件与发布对齐**：Webhook 投递默认每秒调度；Python、npm、Console、SDK、OpenAPI、Compose/Kubernetes 和离线包统一启用 `0.3.0.dev19` / `0.3.0-dev.19`。
- **本地 Redis 排队修复**：`start.py` 在 Redis 队列模式下自动启动 batch/stream worker，并将同一 `.env` 传递给 worker；worker 异常退出会让启动器显式失败，不再出现 API 正常但 Run 永远停在 `queued` 的静默积压。
- **结果统计与提示等级修正**：Console 区分实际采样单元与命中单元，正常媒体 EOF 作为完成提示展示，裁剪图配额不足保留为可操作告警；“时间戳来源”改为整行展示，当前单元特征图片明确标注范围。
- **结果帧完整保存**：浏览器无法播放视频时自动定位到最近有结果帧的命中单元，结果帧不可用说明移出画面；移除 `SCENARA_RUN_ARTIFACT_MAX_FRAMES` 和结果帧数量配额，全部命中单元均保存回放帧，裁剪图仍使用独立配额。
- **升级提示**：已经以旧版 `start.py` 启动的 API 需要重启后才会拉起新 worker；生产 Compose 需确认 `batch-worker` 和 `stream-worker` 均处于运行状态。

验证结果：后端全量测试 `213 passed, 8 skipped`；本次解析链路专项回归 `58 passed`；Console 24 项测试、类型检查、Lint 与生产构建通过；Python/TypeScript SDK、Ruff、Mypy、OpenAPI/SDK 漂移检查和 `git diff --check` 通过。

详细内容见 [0.3.0-dev.19 发布说明](docs/release/0.3.0-dev.19.md)。

## 2026-08-10：0.3.0-dev.18 统一顶栏刷新按钮、人像/OCR领域页标题与工作区按钮布局重构

本版本统一顶栏 Header 刷新按钮与通用 `useRefresh` Hook、重构人像解析与 OCR 文档解析动态顶栏标题与描述、优化解析工作区按钮布局与分段分割线，并启用新的开发版本。

- **全局 Header 统一刷新按钮与 Hook**：在顶栏 `.topbar-actions` 区域内统一放置顶部刷新按钮（位于“已连接”状态按钮左侧），点击时提供转动动画反馈；抽象 `useRefresh` Hook，在 Console 全平台 18 个视图页面统一接入顶部刷新事件，并清理了视图各自重复的局部刷新按钮。
- **领域解析顶栏动态标题与描述**：优化顶栏上下文标题与描述解析逻辑，在访问人像解析（`/parse/portrait`）和 OCR 文档解析（`/parse/ocr`）及其子路径（如图片/视频/文档模式）时，顶部标题明确显示为 **人像解析** 或 **OCR 文档解析**，不再统一显示为通用“解析”；浏览器标签页 `document.title` 同步动态更新。
- **解析工作区按钮布局与分段样式优化**：在人像解析和 OCR 文档解析工作区中，将“**开始解析**”主要动作按钮调整至工作区控制栏（流水线下拉框右侧），紧邻“**查看历史运行**”按钮左侧；去除工作区上方冗余的顶边框分割线；运行历史页面 `RunsView` 支持通过 URL `domain` 参数自动预筛选当前领域的历史记录。
- Python 主包与 Python SDK 启用 `0.3.0.dev18`；npm 根包、Console、TypeScript SDK、镜像和离线包标签启用 `0.3.0-dev.18`。

详细内容见 [0.3.0-dev.18 发布说明](docs/release/0.3.0-dev.18.md)。

## 2026-08-10：0.3.0-dev.17 全表序号边框、导航与总览重构与版本启用

本版本重构总览与解析工作区视觉呈现、为控制台全量数据表格新增显式单元格边框与 `序号` 索引列、完成左侧导航栏 5 大核心功能域重构与紧凑无痕滚动优化，并启用新的开发版本。

- **全量数据表格单元格边框与序号列**：为总览（拓扑/运行/能力）、解析工作台、数据资产、数据集、流水线、模型管理、平台治理、反馈发布、企业专区、审计中心、系统接入及通用解析结果等 20+ 个表格统一补齐 `bordered-table` 样式与 `序号` 列。
- **总览与解析工作区重构**：优化总览页面大卡片与数据表对比呈现；人像与 OCR 解析工作区清理左上角冗余标题与工具栏，将高级跳转整合至顶部配置栏。
- **左侧导航栏分组与高度优化**：导航按 `核心工作区`、`数据与分析`、`智能检索`、`AI 引擎与模型`、`平台治理与系统` 5 大核心功能域收拢；菜单项高度调至 `30px`、Branding 高度调至 `58px` 并配备 4px 极细暗色自定义滚动条，解决大中型屏幕出现垂直滚动条的问题。
- Python 主包与 Python SDK 启用 `0.3.0.dev17`；npm 根包、Console、TypeScript SDK、镜像和离线包标签启用 `0.3.0-dev.17`。

验证结果：Console 全量 Vitest 测试与 Vue 脚本类型检查通过；Vite 生产打包通过；`release_gate.py` 实施契约全覆盖与发布门禁通过。

详细内容见 [0.3.0-dev.17 发布说明](docs/release/0.3.0-dev.17.md)。

## 2026-08-10：0.3.0-dev.16 解析工作区拆分、OCR 视频帧采样与 UI 修复

本版本拆分人像解析与 OCR 文档解析为独立工作区，完善 OCR 对视频与视频流的帧采样参数体系，修复启动兼容性与前端样式缺陷。

- **人像解析**与**OCR 文档解析**从合并的单一页面拆分为独立导航项和工作区视图，侧边栏按领域分组，各自拥有独立路由和工作流。
- 人像领域移除文档 (`document`) 数据类型标签，仅保留图片、视频和实时视频流；OCR 文档领域保留图片、视频和实时视频流三种数据类型。
- OCR 视频与视频流新增专属调优参数：采样策略（定时抽帧 / 镜头切页检测）、采样间隔、镜头切页阈值、最大解析帧数上限；视频流额外支持断线重连次数、连接超时和读取超时。
- 修复跨版本启动时内置 Pipeline 定义更新导致 `StateConflict` 异常的问题；PostgreSQL State Backend 启动时自动执行未应用的 SQL 迁移文件。
- 修复全局 CSS 将 `width: 100%` 和 `min-height: 36px` 错误应用到 checkbox/radio 元素的问题，运行页面"自动刷新"复选框恢复正常尺寸。
- Python 主包与 Python SDK 启用 `0.3.0.dev16`；npm 根包、Console、TypeScript SDK、镜像和离线包标签启用 `0.3.0-dev.16`。

验证结果：全量 Python 测试 `216 passed`（集成环境 8 项 skipped）；Vite 生产构建通过；版本一致性、OpenAPI 契约和发布门禁通过。

详细内容见 [0.3.0-dev.16 发布说明](docs/release/0.3.0-dev.16.md)。

## 2026-08-08：0.3.0-dev.15 跨视频行人重识别闭环与版本启用

本版本把“同一视频内连续出现”推进为“跨视频、跨摄像头的长期身份轨迹”闭环，并将实现、接口、SDK、部署默认标签和发布文档统一到新的开发版本。

- 视频内 tracklet、片段重连和长期轨迹注册器打通；同一人的连续出现可以从单个视频延伸到多个 Run、多个摄像头和长期身份时间线。
- 跨视频 ReID 使用 body + face 多模态融合，叠加摄像头拓扑、最小/最大转移时间和同时间窗口互斥约束；缺少 `recording_started_at` 时不把处理时刻当作跨运行的权威时间。
- 摄像头自动登记、长期身份/片段/时间线查询、人工确认/否决/命名/合并/拆分/删除以及 Console 审查入口形成闭环；公共结果保留身份字段但不泄露原始 embedding。
- `/api/v1/parse/video`、Python SDK、TypeScript SDK、OpenAPI 和部署配置同步支持 `camera_id` 与 `recording_started_at`。
- 新增真实双 AVI 视频 E2E，验证第一段视频创建新身份、第二段视频在另一摄像头完成 ReID，并在身份 API 中汇聚为一个长期身份和两段轨迹。
- Python 主包与 Python SDK 启用 `0.3.0.dev15`；npm 根包、Console、TypeScript SDK、镜像和离线包标签启用 `0.3.0-dev.15`。

验证结果：Docker 启动 PostgreSQL、Redis、MinIO 后，之前跳过的 8 个集成测试全部通过；全量 Python 测试 `216 passed`；OpenAPI/SDK 漂移检查、实现发布门禁和 `git diff --check` 通过。

详细内容见 [0.3.0-dev.15 发布说明](docs/release/0.3.0-dev.15.md)。

## 2026-08-06：0.3.0-dev.14 视频结果精简与告警语义修正

本版本优化人像视频和实时流的结果记录，减少无检测内容的空单元，并修正正常媒体结束被展示为“提前结束”的误导性文案。

- 人像人员检测和完整人像分析流水线仍对全部采样帧执行推理，但视频与实时流中没有检测到任何对象的帧不再写入最终 `result.units`，不会占用结果分页或显示为空记录。
- 保留命中帧的原始 `index`、`pts_ms` 和画面尺寸；`media_metadata.sampled_units` 继续记录实际参与分析的采样帧数，不与最终命中单元数混淆。
- 图片解析继续保留零对象单元，确保单张图片仍返回完整、稳定的结果结构。
- 有限视频正常读取到 EOF 时，Console 将 `media_termination:source_ended` 展示为“媒体源已正常读完”，后端终止原因和审计字段保持不变。
- 特征裁剪图达到 `SCENARA_RUN_ARTIFACT_MAX_CROPS` 单次运行上限时仍采用 best-effort 语义：检测结果不受影响，仅部分对象不生成裁剪图；默认上限保持 200，可按部署容量调整至 0–5000。
- Python 主包与 Python SDK 启用 `0.3.0.dev14`，npm 根包、Console、TypeScript SDK、部署镜像和离线包标签启用 `0.3.0-dev.14`。

验证结果：版本/API/人像分析与产物测试 32 项、实时结果/媒体采样/平台扩展测试 30 项、视频与流 API 测试 5 项、人像身份测试 5 项通过；Console 全量 19 项及格式、Lint、类型检查、生产构建通过，TypeScript SDK、OpenAPI/生成契约漂移检查、Python Ruff、仓库契约门禁、实现发布门禁和 `git diff --check` 通过。

详细内容见 [0.3.0-dev.14 发布说明](docs/release/0.3.0-dev.14.md)。

## 2026-08-05：0.3.0-dev.13 全面中文化与版本同步

本版本主要完成中文用户体验与工程文案的一致性收口，并把当前开发基线同步到 `0.3.0-dev.13`。

- 后端运行时、调度器、工作进程、产品目录和控制面注释统一改为中文，保留协议字段、错误码和技术标识不变。
- 前端控制台的残留英文提示、登录页标题、媒体预览错误和资源加载错误统一改为中文。
- Python/Node 包版本、OpenAPI、SDK、Compose/Kubernetes 镜像标签和离线包默认标签统一升级到 `0.3.0-dev.13` / `0.3.0.dev13`。
- 发布说明、README、策略文档、仓库拓扑、访问底座和实现矩阵同步更新到当前版本基线。
- 新增 `docs/release/0.3.0-dev.13.md` 作为本次升级说明，记录验证结果和后续升级提示。

验证结果：`py -m compileall -q app scenara scripts` 通过，`git diff --check` 通过；Python 全量测试仍因当前环境缺少 `cv2` 依赖而未进入收集阶段。

详细内容见 [0.3.0-dev.13 发布说明](docs/release/0.3.0-dev.13.md)。

## 2026-08-05：0.3.0-dev.12 安全边界、性能与工程结构治理

本版本统一收录前一轮审查后的安全、生产可用性、性能和工程化修改，并升级 Python/npm/Console/SDK/OpenAPI/部署制品版本。

- 授权模型改为 fail-closed，空 scope/product 认证直接拒绝；生产环境强制企业策略，补齐 license、公钥和 Compose secret 配置。
- Session 认证改用 token 摘要索引、恒时比较、节流写入和过期清理；新增 `0011_session_token_index.sql`。
- 审计查询改为数据库分页，导出改为分页流式 JSON/CSV；结果列表批量查 Run，分片并发读取，SSE 连接成功时停止并行轮询。
- Console 集中处理 401 并默认使用 sessionStorage；ParseView 拆出领域目录和媒体预览 composables。
- 审计路由与控制面存储实现拆分，CI 增加并发取消和日志目录治理，Docker 增加 HEALTHCHECK 与 `COPY --chown`。
- 本地 runtime-state 目录由 `scripts/prepare_runtime_state.py` 统一初始化，rollout 审计和 CI 服务日志归档到 `runtime-state/logs/`。

详细内容见 [0.3.0-dev.12 发布说明](docs/release/0.3.0-dev.12.md)。

## 2026-08-05：0.3.0-dev.11 领域能力中文化与环境配置完善

本版本在用户名密码登录基线之上，完成“领域与能力”页面的全面中文化和信息层级优化，并将项目根环境变量模板升级为带完整约束、安全说明和生产部署提示的中文配置文档。版本统一升级为 `0.3.0-dev.11`（Python 包为 `0.3.0.dev11`）。

- 领域名称、说明、流水线名称、版本和状态统一使用中文展示，不再向用户暴露后端英文文案、领域编号或流水线技术标识。
- 页面新增领域与流水线数量概况，并按“支持的数据类型”“已启用能力”“可用解析流水线”组织信息，补齐未知领域、未知流水线和空数据状态。
- 新增标签层单元测试与 Playwright 英文泄漏回归，覆盖后端返回英文显示名、英文说明和自定义流水线编号的情况。
- `.env.example` 的 41 项配置全面补充中文用途、生效条件、可选值、范围、单位和生产风险说明，增加 OCR 工厂配置并清理重复加密密钥项。
- README 增加 `.env` 生成、显式加载和首次管理员配置指引；真实 `.env`、随机密码、根令牌与加密密钥继续由 Git 忽略，不进入版本库。
- Python、npm、Console、两个 SDK、OpenAPI、Compose、Kubernetes、离线脚本、部署示例、实现矩阵和策略文档统一升级到新版本。

本版本不新增公共 API 或数据库迁移，严格发布门禁仍按许可证、真实模型、目标硬件、离线安装和签署证据缺失保持失败关闭。

详细内容见 [0.3.0-dev.11 发布说明](docs/release/0.3.0-dev.11.md)。

## 2026-08-04：0.3.0-dev.10 用户名密码登录与访问控制收口

本版本将当前工作区内的认证、访问控制、控制台、API 契约、SDK、数据库迁移、部署清单和测试优化统一收口，并将版本升级为 `0.3.0-dev.10`（Python 包为 `0.3.0.dev10`）。

- 新增仅使用用户名和密码的 `POST /api/v1/auth/login`，登录页面移除租户和项目登录参数，支持密码可见性切换、保持登录、错误提示、未认证路由重定向和退出登录。
- 用户密码使用独立随机盐的 scrypt 摘要保存；新增 `migrations/0010_user_credentials.sql`，Memory/PostgreSQL 凭据存储保持一致。
- 登录时校验用户状态和 Membership/Role，按项目解析有效 scope 并签发过期会话；会话上下文不能被租户或项目请求头覆盖。
- 新增 `SCENARA_BOOTSTRAP_ADMIN_USERNAME`、`SCENARA_BOOTSTRAP_ADMIN_PASSWORD` 初始化管理员配置，强制成对配置并校验至少 12 位密码。
- Console 接入统一认证状态管理和会话/持久令牌存储，访问工作区支持创建用户密码；品牌文案更新为“视觉 AI 中枢平台”。
- OpenAPI、Python SDK、TypeScript SDK 及生成后的 TypeScript `dist` 制品同步新增登录契约和密码字段。
- 项目生命周期审批、审计保留清理、幂等计量、席位上限、适配器健康探测及 Agent/Flow/Edge/Worker 控制面继续完成 Memory/PostgreSQL、Console、SDK 和文档同步。
- README、产品矩阵、访问底座、仓库拓扑、人像战略、实现矩阵、部署 Compose/Kubernetes/离线脚本和发布门禁统一升级至 `0.3.0-dev.10`。

验证结果：Python `176 passed, 8 skipped`；Console 单元测试 `19 passed`；桌面/移动端 E2E `48 passed`；类型检查、Lint、格式检查、生产构建、SDK/OpenAPI 漂移检查、开发门禁和实现发布门禁通过。严格发布门禁仍按许可证法律批准和签署证据缺失保持 fail-closed。

详细内容见 [0.3.0-dev.10 发布说明](docs/release/0.3.0-dev.10.md)。

## 2026-08-04：0.3.0-dev.9 非模型 P0-P2 控制面完整收口

本版本在不引入真实模型制品、训练作业或模型评估结果的前提下，完成景枢长期战略中剩余的非模型 P0-P2 开发计划，并将全部能力同步到 API、OpenAPI、Python SDK、TypeScript SDK、Console、PostgreSQL 迁移、部署清单和策略文档。

- 新增项目停用、恢复、删除审批请求，支持批准/拒绝决策、跨项目管理员审核和完整审计记录。
- 新增审计保留策略、dry-run/execute 清理、租户/项目隔离查询以及 JSON/CSV 导出控制。
- 新增配额计划、计费账户、幂等 Meter Event、用量聚合和席位上限；支付结算、发票、税务和自助购买继续保留为外部服务门禁。
- 新增 OIDC/SAML/SCIM 身份提供者配置记录和健康探测；交互式会话支持用户状态校验与 Membership/Role scope 解析，真实外部签名断言交换仍需部署适配器。
- 新增标注任务/审核及 Annotation Provider 探测；Index Backend、Search Reranker 注册/列表/探测契约同步完成。
- 新增 Flow 条件/审批/Webhook 执行、Edge 设备/部署/同步确认、Worker 租约与拓扑管理。
- 新增 Agent 工具、最小权限动作、审批执行、Trace、Evaluation 和租户/项目作用域的 TTL Memory。
- Console 新增“平台治理”工作区，统一承载生命周期、审计、计费、适配器健康状态等运营控制。
- 新增 PostgreSQL 迁移 `0009_control_plane_records.sql`，Memory/PostgreSQL 控制面实现同一租户、项目、授权和审计语义。
- 版本统一启用为 Python `0.3.0.dev9`、npm/Console/TypeScript/容器 `0.3.0-dev.9`，并同步更新 OpenAPI、SDK 发布制品、部署示例和 Kubernetes 预生产清单。
- 新增非模型开发门禁 `scripts/development_gate.py`，实现门禁支持 `--implementation-only`；严格发布门禁继续对许可证和签署证据 fail-closed。

验证结果：Python 全量测试 `173 passed, 8 skipped`，Console 检查通过，桌面/移动端 E2E `44 passed`，Ruff、开发门禁、实现发布门禁和 OpenAPI 漂移检查全部通过。

详细内容见 [0.3.0-dev.9 发布说明](docs/release/0.3.0-dev.9.md)。

## 2026-08-03：0.3.0-dev.8 数据治理、审计与保存检索

本版本继续完善除真实模型制品之外的未完成能力：数据集版本治理、资产清单、质量评分、血缘信息、审计查询与导出、保存检索正式进入统一 API、SDK 和 Console。

- 新增数据集目录、版本生命周期和 PostgreSQL 迁移 `0007_dataset_governance.sql`。
- 数据集版本绑定资产、Manifest SHA-256、质量评分、血缘和标注摘要，发布前执行契约校验。
- 新增审计事件分页查询、筛选和 JSON/CSV 导出，始终按租户与项目隔离。
- 新增保存检索资源及迁移 `0008_saved_searches.sql`，文本检索和人像资产检索可保存、复用和审计。
- Console 新增“数据集治理”和“审计中心”工作区，检索页新增保存检索操作。
- Python/TypeScript SDK 与 OpenAPI 同步更新，版本统一为 `0.3.0.dev8` / `0.3.0-dev.8`。

详细内容见 [0.3.0-dev.8 发布说明](docs/release/0.3.0-dev.8.md)。

## 2026-08-03：0.3.0-dev.7 索引契约与完整人像比对

本版本将解析结果、身份库和未来检索能力统一到平台级索引契约，补齐图片注册、图片搜索、图片比对和图片资产比对闭环。控制台新增“检索 / 人像比对”工作区，SDK、OpenAPI、PostgreSQL 迁移和审计语义同步更新。

- 新增 `IndexStore`、索引定义、索引记录、来源引用、向量/文本查询和软删除生命周期；解析结果中的人像对象、OCR 文本块及单元定位信息可被统一消费，索引同时支持来源删除、资产删除和过期回收。
- 新增图片人像注册、图片搜索、两张图片比对、图片资产比对及图片/资产混合比对 API；响应只返回模型和质量摘要，不返回原始 embedding。
- 图片搜索以人像索引为主查询路径，并新增通用索引向量查询接口，为人搜图、文搜图、文搜视频复用统一契约。
- 新增统一检索服务和 `/api/v1/search/text`、`/api/v1/search/image`，支持文搜图、文搜视频、人搜图、数据类型过滤、来源定位和结果中心回溯。
- 新增 `/api/v1/search/asset`，可直接从数据资产选择查询图片；人像图片注册、搜索和比对严格校验特征空间的模型 ID、模型版本和向量维度，原件过期后禁止继续读取。
- 检索命中结果中心时保留运行编号和解析单元定位，视频帧、文档页可直接打开对应单元，减少跨页面二次查找。
- 人像特征空间和身份删除保持租户/项目隔离，并同步清理生物特征和索引来源记录；索引故障会显式反映为结果 `partial`。
- Python/TypeScript SDK 和 Console 工作区同步支持新接口；版本统一升级为 `0.3.0.dev7` / `0.3.0-dev.7`。

详见 [0.3.0-dev.7 发布说明](docs/release/0.3.0-dev.7.md)。

## 2026-08-03：0.3.0-dev.6 解析产品化与全局结果中心

本版本将解析能力从分散的资源、运行和单次结果页面整合为领域工作区与全局结果中心。人像和 OCR 文档保持独立领域上下文，图片、视频、文档和视频流在工作区内部切换；“媒体”正式更名为“数据资产”。版本号统一为：Python 主包与 Python SDK `0.3.0.dev6`，npm 根包、Console、TypeScript SDK 与默认容器标签 `0.3.0-dev.6`。

### 产品信息架构与用户体验

- 左侧解析导航只展示人像、OCR 文档等领域，不再重复展开图片、视频、文档、视频流三级菜单。
- 每个领域工作区连续承载数据选择、采样和领域参数、运行进度、当前结果与同类型历史，减少跨页面操作和上下文丢失。
- 具体领域页面不再重复显示人像/OCR 切换控件；数据类型只在工作区内部以标签切换。
- 已完成任务进入解析结果中心，运行中任务返回对应工作区；工作区、运行中心和结果中心之间建立明确的双向入口。
- 保留 `/parse/{domain}`、`/parse/{domain}/{media_kind}` 深链，以及旧人像、OCR 和查询参数入口的兼容跳转。

### 数据资产

- 用户侧“媒体”菜单和页面更名为“数据资产”，统一管理图片、视频、文档文件与视频流来源。
- 页面内部使用“文件资产”“视频流来源”“数据类型”等面向用户的名称，避免把底层 `media` 技术概念暴露为产品分类。
- 正式入口改为 `/assets`，旧 `/media` 自动跳转；后端 API 和数据库字段暂时保持兼容，不引入破坏性迁移。

### 解析结果中心

- `/results` 升级为跨领域结果中心，支持按领域、数据类型、资源名称、资产编号、来源编号和运行编号查找结果。
- 结果列表与详情联动展示 OCR 文本、人像对象、特征图片、解析单元、告警和原始结果信息。
- 新增 `GET /api/v1/results` 分页接口，以及 `ResultSummary`、`ResultSummaryPage` 和可检索结果引用摘要。
- Memory/PostgreSQL 状态实现统一筛选、计数和稳定排序；Run 完成时写入轻量摘要，列表查询不加载完整结果对象。
- 新增 `0005_result_center_indexes.sql`，覆盖领域、创建时间、数据类型和资源名称查询，并原子记录迁移版本。
- 结果中心为后续以人搜图、文搜图、文搜视频和人像比对提供统一入口；本版本不把尚未实现的向量召回、全文检索和比对策略列为已交付能力。

### 契约、兼容与发布

- Python、npm、Console、Python SDK、TypeScript SDK、OpenAPI 和默认容器标签统一升级到 dev.6。
- 旧结果缺少新增摘要字段时仍可查看详情；新结果自动写入完整摘要。
- 标准 Compose 通过迁移服务自动应用 `0005_result_center_indexes.sql`。
- 新增独立的 [0.3.0-dev.6 发布说明](docs/release/0.3.0-dev.6.md)，同步 README、实现矩阵、产品矩阵、访问底座、仓库拓扑、人像战略和全面优化方案的适用版本。

### 验证

- Python 全量回归 153 passed、8 skipped。
- Console 单元测试 18 项通过，格式、Lint、类型检查与生产构建通过。
- 桌面端和移动端 Playwright 端到端测试 36 项通过；TypeScript SDK 检查通过。
- Docker 调试环境完成迁移并健康启动，`/readyz`、`/api/v1/results`、`/console/results` 和 `/console/assets` 均返回 HTTP 200。

## 2026-08-03：0.3.0-dev.5 领域媒体解析工作区

本版本将解析控制台从单一大页面调整为“解析 -> 领域”的工作区结构，并补齐全项目解析结果中心。人像和 OCR 文档分别进入独立领域页面，图片、视频、文档和视频流在页面内部切换；用户可以在同一领域上下文内完成上传、参数配置、解析进度、当前结果查看和历史运行，同时可以从“解析结果”统一浏览跨任务结果。文件和视频流来源入口统一称为“数据资产”。版本号统一为：Python 主包与 Python SDK `0.3.0.dev5`，npm 根包、Console、TypeScript SDK 与默认容器标签 `0.3.0-dev.5`。

### 领域与菜单

- 解析菜单改为由 `DomainManifest` 动态生成的领域二级导航，人像和 OCR 文档页面相互独立，媒体类型不再在侧栏重复展开。
- 保留 `/parse/{domain}` 领域入口和 `/parse/{domain}/{media_kind}` 媒体深链，并保留旧版人像、OCR 和查询参数入口的兼容跳转。
- 媒体类型仅在当前领域工作区内部展示，为未来车辆、工业、安防等领域预留扩展边界。
- 保留“媒体”和“运行”作为资源管理、跨领域查询入口，日常解析流程不再依赖页面跳转。
- 将用户侧“媒体”菜单更名为“数据资产”，页面内区分文件资产和视频流来源，保留 `/media` 到 `/assets` 的兼容跳转。

### 全局解析结果中心

- `/results` 从已完成运行的重定向改为真实的解析结果中心，提供领域、数据类型、文件名/来源和运行编号筛选。
- 新增 `GET /api/v1/results` 结果摘要分页接口；保留 `/api/v1/runs/{run_id}/result` 作为单次结果详情接口。
- 结果引用持久化资源名称、数据类型、解析单元、对象、人像、文本块、文本长度和告警摘要，为后续统一检索和人像比对建立稳定索引边界。
- 解析工作台和运行中心均可直接进入结果中心，避免上传、运行状态和结果查看之间形成割裂流程。

### 解析工作区

- 解析页新增当前领域/媒体类型上下文栏，媒体类型切换统一收敛到工作区标签，避免重复入口。
- 图片、视频、文档和视频流页面统一承载上传、媒体库、参数、运行控制、结果和当前媒体类型历史。
- 历史运行直接展示在当前媒体工作区，并支持恢复对应 Run。
- 前端对 API 暂不可用和不完整列表响应增加安全归一化，保持清晰的空状态。

### 验证与发布

- Console 单元测试 18 项通过，桌面与移动端端到端测试 34 项通过。
- 类型检查、Lint、构建和格式检查全部通过。
- Docker 调试镜像按 `0.3.0-dev.5` 重建，隔离栈 `scenara-debug-dev5` 在 `127.0.0.1:8012` 健康运行。
- 图片 Run `run_a5d151f0621c40dea5d821a6d2959865` 完成，视频 Run `run_689cd03522e04e1da5465e70e525a9b8` 完成，视频流 source `src_216e30a244724ed49011ae8216fabf38` 探测可达且 Run `run_e60e1d722f054c8e92c78e264e0b1e09` 完成。
- 原调试数据卷因保存旧流水线定义而触发不可变校验，本次没有清空旧数据，使用新隔离卷完成实际验证。


## 2026-08-03：0.3.0-dev.4 迁移收缩、存储分页与质量门禁

本版本承接 `0.3.0-dev.3`，集中修复迁移层失控、容量路径全量加载、覆盖率盲区、跨平台契约摘要和领域扩展边界，并收录解析后特征图片能力。版本号统一为：Python 主包与 Python SDK `0.3.0.dev4`，npm 根包、Console、TypeScript SDK 与默认容器标签 `0.3.0-dev.4`。本版本仍是工程开发快照，不代表严格 `1.0.0` 发布门禁已通过。

### 迁移层收缩与生产攻击面

- 删除 116 个不被 `scenara/`、测试、SDK、脚本、Dockerfile、部署目录或 CI 配置引用的旧 `app/` 模块，包括已退役的人像旧版路由、`app.server`、OIDC、旧 PostgreSQL 适配器、流作业、图库和商业化路径。
- `app/` 从 157 个 Python 文件、约 43,364 行收缩为 43 个实际可达文件、约 6,882 行；Docker `COPY . .` 不再把已退役业务表面带入生产镜像。
- 拆除 `app.media`、`app.runtime` 和 `app.vision` 聚合导入副作用；公共向量计算和异常摘要提取为小型独立模块，推理代码直接依赖实际使用的预处理、后处理和运行组件。
- 新增迁移层可达性门禁：基于 AST 和部署配置字符串构建引用图，新增不可达模块会导致架构测试失败，形成“只能收缩或有证据增长”的约束。

### 存储分页与容量路径

- `list_assets`、`list_sources`、`list_runs` 的过滤、排序、偏移和数量限制下推到 Memory/PostgreSQL 存储实现；列表 API 不再先读取租户项目下全部记录再在 Python 中切片。
- 资产、来源和 Run 总数改用独立 `COUNT`；分页数据与总数通过 `asyncio.gather` 并发获取。
- 删除资产或来源前通过数据库 `EXISTS` 检查未终态 Run，替代全量加载与逐行 Pydantic 校验。
- 新增 `migrations/0004_state_list_indexes.sql`，补齐资产/来源创建时间排序索引和按资产/来源定位未终态 Run 的部分表达式索引。
- 新增内存存储分页回归与 PostgreSQL 真实服务集成用例，覆盖过滤、总数、稳定顺序和活动引用判断。

### 真实覆盖率与 Python 质量门禁

- `app/` 纳入完整 Ruff、Mypy 和 pytest coverage，不再使用缩减的 `E4/E7/E9/F` 检查；全局恢复 `E501` 行长规则。
- 覆盖率证据现在包含 `scenara`、剩余 `app` 推理代码和 Python SDK：11,465 条语句，实测 62.84%，CI 门禁设为 60%。此前约 81% 的部分范围数据不再作为交付物质量证明。
- 全量 Python 回归为 150 passed、8 skipped；跳过项均声明为需要 PostgreSQL、Redis 或 S3 兼容服务的集成测试，并由 CI 真实服务作业执行。

### 架构边界与第三领域扩展

- 架构扫描从 `scenara/platform/*.py` 扩大到全部 `scenara/**/*.py`，只允许 `scenara/domains/portrait` 导入迁移层；`scenara.server` 通过领域能力适配器获取 Portrait 快照，不再直接导入 `app.*`。
- 默认 Pipeline、产品范围和资格证据类型由 Domain Plugin Manifest 提供，服务器、产品目录和反馈服务不再包含 Portrait/OCR 条件分支。
- 结果模型保留 Portrait/OCR 强类型 payload，并增加可扩展的通用领域 payload；顶层领域与 payload 领域必须一致，第三领域无需修改平台闭合联合即可返回结果。
- 架构测试新增平台领域条件分支扫描、第三领域 payload 行为验证和 `source-manifest.json` 真实边界校验，移除仅检查 README 关键词的空门禁。

### 契约摘要、构建产物与前端格式

- 新增 `.gitattributes`，强制 `contracts/repository/**/*.json` 使用 LF；Windows `core.autocrlf=true` 不再改变已发布契约字节和 SHA-256。
- TypeScript SDK `dist/` 明确为 npm 发布制品并固定 LF；Console `dist/` 继续作为部署时生成物忽略，二者策略写入 `.gitignore` 注释。
- Console 引入 Prettier 3.9.6 及格式漂移门禁，11 个视图全部重排，最长行从 1,511 字符降为 124 字符。
- ESLint 继续负责语义规则，Vue 模板缩进和闭合括号由 Prettier 唯一负责，消除 632 条相互冲突的格式警告。

### Console 媒体与运行状态连接

- Console 内容安全策略为 `img-src` 和 `media-src` 同时放行 `blob:`，本地选择的图片、视频预览可以正常加载，同时保持外部媒体默认拒绝。
- Run SSE 响应禁止代理缓存、内容转换和缓冲；轮询对短暂网络中断执行四次有限退避重试，业务错误仍立即返回。
- 调试 Compose 改用同拓扑内的 PostgreSQL 状态后端并自动迁移；`debug-postgres` 卷持久化 Run/事件，`debug-state` 卷持久化本地对象文件。API 重启后前端恢复轮询，内联队列按原 `run_id` 重交非终态 Run。
- 修复实测发现的调试环境 SecretStore 生命周期不一致：流元数据此前写入 PostgreSQL，但流地址只存在进程内存，API 重建后会留下不可执行的流源。调试 Compose 现在用加密对象 SecretStore 将密文写入 `debug-state` 卷，并支持通过 `SCENARA_DEBUG_SECRET_ENCRYPTION_KEY` 固定本机密钥；生产密钥要求不变。
- Linux 部署脚本通过 `.gitattributes` 固定为 LF，防止 Windows CRLF 破坏容器内迁移入口，并新增部署契约门禁。

### 视频解析内存与批处理边界

- 修复长视频解析的真实 OOM 根因：解码层此前让全部采样帧以原始 RGB 常驻内存，人体检测又一次性构造全部 640×640 YOLO 输入；大采样量下两次放大会让容器被 Docker 以 OOM/137 终止，从而表现为 SSE `ERR_INCOMPLETE_CHUNKED_ENCODING`、Run 查询 `ERR_EMPTY_RESPONSE`，内存状态后端重启后最终再表现为 404。
- 视频解码现在根据媒体尺寸、采样策略、时间窗口和预计单元数自动计算有效最大边长，把单 Run 解码后 RGB 帧预算上限固定为 512 MiB，并通过 `media_metadata.frame_max_edge` 返回实际值。
- `portrait.person-detection` 算子升级到 `1.1.0`，按最多 16 帧调用 YOLO，累计批次时延而不再创建全视频推理张量。
- 使用原故障 Run 对 1920×1080、662.6 秒视频执行恢复验证：663 个采样帧全部进入推理，自动最大边长 692 像素，容器峰值约 2.61 GiB，Run 完成，重启计数与 OOM 事件均为 0；CSP 实际浏览器验证中 Blob 图片完整加载为 64×64，控制台无错误。

### 视频与实时流实时解析

- `platform.media.decode` 升级为 `1.2.0`。视频文件和实时流不再先完整解码到内存再交给模型，而是由后台解码线程通过有界队列逐批交给人员检测；有界队列同时提供反压，暂停、取消、模型不可用和任务异常都会停止解码线程。
- 人员检测对视频文件每 8 个分析单元、对实时流每个分析单元发布进度与部分结果。`Run.progress` 在数据库中使用乐观锁单调更新，SSE 发送 `run.progress` 和 `result.partial`，包含已处理单元数、预计单元数和当前百分比。
- Run 未完成时，现有结果接口已经可以读取最新的部分 `ResultEnvelope`。每次快照使用新对象键写入并原子替换数据库引用，替换成功后删除上一份临时快照；完成时写回正式结果路径，失败或取消则清除未完成快照。
- 解析工作台收到事件后立即刷新进度条、单元/对象计数、时间轴和特征图片，并自动跟随最新单元；用户点选历史单元后保留其选择。SSE 短暂断开时，轮询按进度变化补取部分结果。
- 结果页按 20 个单元分页覆盖完整最终结果。每个包含检测对象的单元均提供“查看特征图片”，裁剪图可点击打开完整帧并显示检测框，文件视频和实时流使用相同产物契约。
- Docker + 真实浏览器验收使用 1920×1080 HEVC 人物视频：文件 Run `run_915a658dcd69469a81877c21ee2f22ab` 在完成前显示 27%/8、50%/16、74%/24，最终 32 单元、21 个对象；HTTP MPEG-TS 流 Run `run_2e3c39dd7f6b4c4aa65f27b3820a277c` 从第 1 到第 8 单元逐个发布，运行中显示 27%、38%、50%、62%、74%、85%，最终识别 7 个对象。
- 浏览器逐图验证视频和流裁剪图均已解码，点击后完整帧均为 1920×1080 且检测框高亮存在；两个 Run 都能在结果页读取全部最终单元并重复打开图片，页面诊断日志为空。
- 修复实测发现的状态乱序：轮询已经观察到终态后，较旧的 SSE `run.running` 不再让 UI 回退；未终态进度最高显示 99%，只有终态可以显示 100%。
- 修复新建流源的连接竞争：创建完成后不再立即自动请求首帧预览，避免预览先占用只允许单客户端的流并导致随后启动的解析失败；从列表主动选择已有流源时仍保留按需预览。
- 新建流源 `src_1cd455c67c7548cdad6aa9f35f1ed63a` 后强制重建 API 容器，重启后首帧预览成功，复用同一来源启动的 Run `run_9816db5634ec4b13a057e36566901224` 在运行中显示 50%/4，最终完成 8 个单元并识别 9 个对象，证明流地址密文和本机调试密钥能够跨容器生命周期恢复。

### 版本启用与发布制品

- Python 主包、Python SDK、API 系统状态和 Python SDK User-Agent 升级到 `0.3.0.dev4`。
- npm 根包、Console、TypeScript SDK、部署示例、Compose 默认镜像标签和离线包默认标签升级到 `0.3.0-dev.4`。
- 新增 `docs/release/0.3.0-dev.4.md`，并同步 README、实施矩阵、产品矩阵、访问底座、仓库拓扑、人像智能战略和全面升级方案的当前适用版本。

### 特征图片产出与查看

- 人像解析过程中，为每个检测到的人员、人脸和轮廓按检测框裁剪出独立的特征图片，并为该分析单元保存一张完整原图，两者都以 JPEG 写入对象存储。
- 图片、视频文件、PDF 文档和实时流四类入口全部覆盖；实时流没有可回放的原始媒体，正因为裁剪在解析时完成，流解析结果同样可以查看特征图片。
- 控制台解析工作台新增“特征图片”面板：默认以网格显示当前分析单元中每个对象的裁剪图，标注对象类型和置信度；点击任意裁剪图弹出大图，显示该对象所在的完整原图并高亮其检测框，支持 Esc 关闭、← → 在对象间切换。
- “结果”页的媒体单元表格增加“查看特征图片”入口，历史运行刷新后仍可回看，不依赖浏览器本地缓存的原始文件。

### 契约与接口

- `VisionObject` 新增 `crop_artifact_id`，`MediaUnitResult` 新增 `frame_artifact_id`，均指向 `ResultEnvelope.artifacts` 中已有的 `ResultArtifact` 记录。既有字段没有变化，旧客户端不受影响。
- 新增 `GET /api/v1/runs/{run_id}/artifacts/{artifact_id}`，沿用运行读取权限，返回带 `ETag` 与私有缓存头的图片。只有结果中声明过的产物标识可读，未声明或伪造的标识一律返回 404，因此不存在通过该接口穿越对象存储路径的可能。
- Python SDK 新增 `get_result_artifact()`，TypeScript SDK 新增 `getResultArtifact()`；OpenAPI 与生成的 SDK 类型同步更新。

### 存储、配额与保留期

- 特征图片按派生预览归类，跟随 `SCENARA_PREVIEW_RETENTION_DAYS`（默认 30 天）自动清理，不使用永不过期的生物特征保留策略。
- 运行产物使用 `SCENARA_RUN_ARTIFACTS_ENABLED`、`SCENARA_RUN_ARTIFACT_MAX_CROPS`、`SCENARA_RUN_ARTIFACT_CROP_MAX_EDGE` 和 `SCENARA_RUN_ARTIFACT_FRAME_MAX_EDGE` 控制；结果帧数量自 0.3.0-dev.19 起不再设置每 Run 上限，旧 `SCENARA_RUN_ARTIFACT_MAX_FRAMES` 配置已移除。
- 超出配额时结果中记录 `artifact_quota_reached` 告警并在控制台给出中文说明；对象存储写入失败记录 `artifact_storage_unavailable`，但不会影响解析结果本身。
- 运行失败或被取消时，已写入的特征图片会被删除，不留孤儿对象。
- OCR 域本次不产出特征图片：文本块没有裁剪图入口，单独生成页面大图会消耗存储却没有消费方。契约字段加在通用的 `VisionObject` 上，OCR 后续需要时可直接复用。

### 测试与验证

- 新增 `tests/test_run_artifacts.py`：产出与契约一致性、接口读取与 ETag、未知标识与跨租户拒绝、开关关闭、配额告警、失败清理，以及裁剪与编码的边界行为。
- 新增 `tests/integration/test_run_artifacts_services.py`：在真实 PostgreSQL 与 MinIO 上跑通特征图片全链路，校验产物可读、保留期记录归类为 `preview` 且归属运行结果，并验证保留期到期后由清理器从对象存储真实删除、删除后读取接口返回“未找到”而不是抛出底层存储错误。
- 控制台新增特征图片渲染与大图交互用例，并补齐此前 `parse-workbench.test.ts` 中缺失的图片接口 mock。
- Docker 实测：构建生产镜像后，容器接 PostgreSQL 与 MinIO、挂载真实 YOLOv8n 模型并启用 GPU，解析一张含多人的照片检出 4 人，产出 4 张裁剪图和 1 张帧大图并确认落入 MinIO；逐张像素比对确认每张裁剪图对应各自检测框区域；真实浏览器打开容器提供的控制台，确认裁剪图网格渲染、点击弹出大图原图、高亮框定位准确、键盘切换与关闭可用，浏览器控制台无错误。
- 新增长视频解码内存预算和人体检测批处理回归，避免容量修复只依赖本机观测。

### 综合验证与发布边界

- Python 全量测试：153 passed、8 skipped；真实合并覆盖率 63.05%（11,647 条语句），60% 门禁通过。
- Ruff 全范围检查通过；Mypy 检查 103 个源文件无问题。
- Console Prettier、ESLint、17 个 Vitest、Vue 类型检查和生产构建全部通过；桌面/移动 Playwright 34 passed。
- OpenAPI、TypeScript SDK、仓库契约摘要与打包无漂移；`repository_gate.py` 和 `release_gate.py --implementation-only` 通过。
- 升级 PostgreSQL 环境必须执行 `0004_state_list_indexes.sql`；标准 Compose 迁移任务会自动执行。已声明不兼容的人像旧版路由和 `app.server` 已删除，内部调用方必须迁移到 `scenara.server` 与当前 `/api/v1`。
- 严格发布门禁仍按设计失败关闭：法务许可证批准、九类具名签署证据和目标环境资格报告尚未齐备，`0.3.0-dev.4` 不宣称具备 `1.0.0` 生产发布资格。

---

## 2026-07-31：0.3.0-dev.3 媒体预览、GPU 验证与运行契约修复

本版本承接 `0.3.0-dev.2`，重点修复图片、视频文件和视频流在控制台中无法显示的问题，并完成 GPU 推理链路的本地 Docker 实测。版本号统一为：Python `0.3.0.dev3`，npm、Console、TypeScript SDK 与默认容器标签 `0.3.0-dev.3`。本版本仍是工程开发快照，不代表严格 `1.0.0` 发布门禁已通过。

### 媒体预览与解析结果

- 图片选择后使用可渲染的 Data URL 预览；浏览器无法直接加载时，自动回退到文件读取结果，避免只显示破损图片占位符。
- 图片和视频解析后，通过服务端媒体预览接口获取可渲染的 JPEG 首帧；视频浏览器不支持原始编码时，控制台仍能显示首帧并保留解析结果。
- 新增视频流源首帧预览接口，流解析结果页显示首帧和检测框叠加；预览失败不影响流任务执行和结果查询。
- MediaView 的视频预览明确为“视频首帧预览”，不再把 JPEG 响应错误地交给 `<video>` 播放器。
- 新增 `GET /api/v1/media/sources/{source_id}/preview`，沿用来源权限、外部 URL 校验、私网访问策略和超时限制。

### 解析单元与告警说明

- “解析单元”是系统实际送入解析流水线的媒体片段：图片通常为 1 个单元，视频/视频流按采样策略产生帧单元，PDF 按页面产生单元；它不是检测到的人数或对象数量。
- `max_units_reached` 现在在控制台中明确显示为达到上限后主动正常结束，并提示提高“最大分析单元”或缩小采样间隔。
- 人像结果当前展示源图/首帧、检测框、置信度和数值特征记录；特征向量不是图片，当前版本没有单独的裁剪特征图查看入口，也不会在本地自动生成一个可浏览的“特征图片”目录。

### 模型、Docker 与 GPU

- 默认人像模型配置统一使用运行时缓存键 `scenara.portrait/yolov8n` 和 `scenara.portrait/osnet_ibn_x1_0`，能力配置通过 `person_detector_default`、`person_reid_default` 别名绑定，避免点号模型标识无法命中缓存。
- Dockerfile 调整前端源码复制顺序，确保依赖安装和 Console 构建使用完整工作区源码。
- 在本地 Docker GPU 环境完成实测：NVIDIA GeForce RTX 3070 Laptop GPU 可见，ONNX Runtime 使用 `CUDAExecutionProvider`（同时注册 TensorRT、CUDA、CPU provider），模型日志确认 `device_id=0`，`/livez` 和 `/readyz` 均返回 200。

### 测试与质量

- Python 平台与部署契约回归：30 passed；Ruff 检查通过。
- Console 类型检查、Vitest、Lint 和生产构建全部通过。
- Docker GPU 容器中完成图片、H264 视频和视频流首帧预览实测，浏览器控制台无错误或警告。
- CI 的集成服务引导拆分为依赖服务启动、数据库迁移和对象存储初始化三个独立步骤；三项幂等操作增加最多 3 次有限重试，启动失败时输出容器状态和末尾日志，避免瞬时 Docker/健康检查失败只留下笼统退出码。

---

## 2026-07-31：0.3.0-dev.2 解析工作台与发布收口

本版本将媒体解析、控制台、API/SDK 契约和发布证据整理为可交付的开发版本。版本号统一为：Python `0.3.0.dev2`，npm、Console、TypeScript SDK 与默认容器标签 `0.3.0-dev.2`。本版本仍是工程资格快照，不代表严格 1.0 发布门禁已通过。

### 解析能力与运行内核

- 图片、视频文件、PDF 文档和视频流四类入口统一使用可复现的 Pipeline/Run/Result 契约。
- 视频支持 `interval`、`keyframe`、`scene_change`、`uniform` 四种采样策略、时间窗口、关键帧交叉验证、帧最大边长缩放和 PDF `page_scale`。
- 文件视频优先使用解码器 PTS，实时流使用跨重连单调作业时钟，并记录 `timestamp_source`、重连次数、seek 使用情况、采样计数和耗时等元数据。
- 解码、重连、退避、PDF 页面处理和长算子持续响应暂停/恢复/取消；暂停时间不再计入算子超时。
- 取消请求持续跟踪到稳定终态，避免控制台停在 `cancelling` 中间状态。
- 服务端在省略版本时解析唯一 active Pipeline；显式版本仍用于可复现绑定。

### API、SDK 与契约

- 新增独立 `ParseDocumentResponse`，不再复用视频响应类型。
- 产品目录显式列出 `/api/v1/parse/image`、`/video`、`/document`、`/stream` 四个解析入口。
- Python SDK 图片快捷接口补齐 `pipeline_id`、`pipeline_version`、`idempotency_key`；视频、文档和流接口支持省略版本并由服务端选择 active 版本。
- TypeScript SDK、OpenAPI、生成代码和导出类型同步更新，所有漂移检查保持通过。

### 控制台

- ParseView 支持图片、视频、PDF、视频流四种模式，包含采样、窗口、帧缩放、重连/连接/读取超时、SSE 接管、暂停、恢复、取消、结果导出和告警查看。
- MediaView 从 Pipeline 目录读取当前 active 版本，不再硬编码旧版本。
- 运行管理和结果查看保持统一 Run 状态、进度、耗时和结果入口；桌面与移动视口均完成 E2E 验收。

### 发布和质量证据

- 新增 `docs/release/0.3.0-dev.2.md`，同步实现矩阵、支持矩阵、产品矩阵、访问底座、仓库拓扑和人像智能战略文档版本。
- 发布说明记录实际验证命令和结果：Python `124 passed, 6 skipped`、覆盖率 `80.42%`、Console Vitest `13 passed`、Playwright `34 passed`、真实服务集成 `6 passed`。
- Ruff、Mypy、OpenAPI/SDK 漂移、仓库契约、仓库门禁、实现门禁、pip-audit 和 pnpm audit 均通过。
- 严格发布门禁继续失败关闭，原因仅为法律批准的 `LICENSE`、九类签署证据和目标环境资格报告尚未由责任人提供；未用占位材料伪造通过。

- 修复 Gitleaks 将公开的 `OPENAPI_SHA256` 契约摘要误报为密钥：生成器在源码、运行时代码和 `.d.ts` 声明中均保留同一行的精确 `gitleaks:allow` 标记，并以回归测试防止 TypeScript 编译后再次丢失。
---

## 2026-07-31：人像智能基础平台战略契约层

本次更新把 `docs/strategy/PORTRAIT_INTELLIGENCE_STRATEGY.md` 描述的六大核心模块、三项战略资产与七项能力就绪度，从散文文档变成**机器可读、可门禁、Console 可见**的平台契约，完全对齐现有 `product_catalog` / `access_foundation` / `repository_topology` 契约体系。

### 新增 `GET /api/v1/platform/portrait-intelligence`

返回 `PortraitIntelligenceStatus`（契约版本 `1.0`），包含三个字段：

- `modules` — 六个战略模块（数据治理、标注平台、模型训练、人像算法、向量检索、模型运维），每项声明成熟度、仓库归属、当前范围与下一道门禁。
- `assets` — 三项战略资产（人像数据湖、人像基础模型、人像智能引擎），`depends_on_modules` 引用经契约校验。
- `capabilities` — 七项能力就绪度（`person_detection`、`body_embedding`、`face_detection`、`face_embedding`、`pose`、`gait`、`appearance`），从运行时 `model-capabilities.yml` 实时派生，不硬编码。

成熟度枚举：`available` / `partial` / `seed` / `planned` / `external`。`external` 表示职责在其他仓库（如训练在 `scenara-model`），如实记录差距不隐藏模块。

### 平台模型

`scenara/platform/models.py` 新增 `PortraitModuleMaturity`、`PortraitCapabilityReadiness`、`PortraitCapabilityItem`、`PortraitModuleItem`、`PortraitAssetItem`、`PortraitIntelligenceStatus` 六个类型，均带完整 Pydantic 校验：模块/资产 ID 唯一、`depends_on_modules` 引用必须存在、能力 ID 唯一。

### 纯函数构建器

`scenara/platform/portrait_intelligence.py` 是无副作用纯函数，能力快照由调用方注入，`scenara/platform` 不导入 `app.*`，通过 `tests/test_architecture.py` 架构约束测试。

### Python SDK

`scenara_sdk` 追加 `PortraitIntelligenceStatus`、`PortraitModuleItem`、`PortraitAssetItem`、`PortraitCapabilityItem` TypedDict 及 `get_portrait_intelligence()` 方法。

### TypeScript SDK

`types.ts` 追加对应接口与类型别名，`client.ts` 新增 `getPortraitIntelligence()` 方法，`index.ts` 补全导出，`generated.ts` 随 OpenAPI 重新生成。

### Console 总览面板

`OverviewView.vue` 在产品矩阵面板与仓库拓扑面板之间新增**"人像智能基础平台"面板**，分三区展示：

- 六大模块网格（三列，紫色顶边，成熟度徽章）
- 七项能力就绪度横列（彩色就绪徽章）
- 三项战略资产网格（三列，琥珀色顶边）

服务端英文字段全部通过 `labels.ts` 转为中文，新增 `labelPortraitModule` / `labelPortraitMaturity` / `labelPortraitReadiness` / `labelPortraitAsset` / `labelPortraitCapability` 及对应 Summary / Gate 函数。

### 测试

- `tests/test_api.py`：`test_platform_portrait_intelligence_contract` — 端点 200、六模块唯一性与仓库归属、三资产引用完整性、七能力就绪度类型校验、数据治理/标注不得标为 `available`
- `tests/test_python_sdk.py`：SDK `get_portrait_intelligence()` 方法调用与返回类型
- `frontend/console/tests/api.test.ts`：所有新增 labels 函数的中文断言（防英文泄漏回归）
- `frontend/console/e2e/workspaces.spec.ts`：总览路由 mock 补入 `portrait-intelligence` 端点，英文后端字段不得渗入界面

### 验证

```sh
python -m pytest -q                  # 116 passed, 6 skipped
python -m ruff check scenara sdk/python tests   # 无问题
python -m mypy scenara sdk/python               # 58 文件无错
python scripts/export_openapi.py --check        # 无漂移
python scripts/generate_typescript_sdk.py --check  # 无漂移
npm run console:check                # lint + 7 tests + typecheck + build 全绿
```

---

## 2026-07-31：0.3.0-dev.1 媒体解析全面升级

本版本对三类媒体解析能力进行了业界前沿水准的全面完善，涵盖后端解码引擎、REST API、Vue 控制台工作台、类型契约和测试覆盖。

### 视频与实时流抽帧能力

- **新增四种采样策略**：`interval`（固定时间间隔，原有）、`keyframe`（关键帧）、`scene_change`（场景切换，基于灰度直方图相关性）、`uniform`（均匀分布）。
- **时间窗口**：`sample_start_ms` 和 `sample_end_ms` 限定采样范围；文件类视频优先使用 `CAP_PROP_POS_MSEC` seek 跳帧，失败时回退到逐帧跳过；终止原因新增 `sample_window_completed`。
- **帧缩放**：`frame_max_edge` 参数对解码后每帧等比缩放，减少内存占用并保护下游算子输入尺寸。
- **PDF 渲染**：`page_scale` 参数（默认 1.5×）控制 pypdfium2 渲染倍率；`_decode_pdf` 补全 `width`/`height` 元数据和 `termination_reason`。
- **可观测性**：媒体元数据新增 `sample_strategy`、`sample_start_ms`、`sample_end_ms`、`keyframe_count`、`scene_change_count`、`frame_max_edge`、`decode_seek_used`、`reconnect_count`、`elapsed_ms` 等字段，全部落入 `ResultEnvelope.media_metadata`。

### 数据模型

- `MediaTechnicalMetadata` 新增上述 11 个字段（全部可选，向后兼容）。
- `models.py` 新增 `SampleStrategy` 枚举（`interval` / `keyframe` / `scene_change` / `uniform`）。
- `media_batch.py` 新增 `SamplePlan` 冻结数据类，集中管理采样参数并在调用入口统一校验。
- `DecodeMediaOperator` 版本升级为 `1.1.0`，透传所有新参数；`decode_media()` 公共签名扩展同步参数，旧代码无须修改。
- `_downscale()` 辅助函数统一处理图片与帧的等比缩放。

### REST API

- `POST /api/v1/parse/video` 新增表单参数：`sample_strategy`、`sample_start_ms`、`sample_end_ms`、`scene_change_threshold`、`frame_max_edge`、`page_scale`。
- **新增** `POST /api/v1/parse/document` 端点：专门处理 PDF 文档，接受 `max_units`、`page_scale` 参数，返回 `ParseVideoResponse`（异步 Run + 结果）。
- `portrait.person-detection`、`portrait.analysis`、`ocr.document` 流水线的 `allowed_parameters` 白名单同步扩展，新增 `sample_strategy`、`sample_start_ms`、`sample_end_ms`、`scene_change_threshold`、`frame_max_edge`、`page_scale`；OCR 流水线还新增 `min_score` 和 `language_hint` 保留位。
- OpenAPI Schema（`docs/openapi.json`）与 TypeScript SDK（`sdk/typescript/src/generated.ts` 及 dist）已重新生成并同步。

### 控制台工作台（ParseView）

- **新增 PDF 模式**：媒体类型选择器增加"PDF 文档"标签页，文件选择器限定 `.pdf`，参数区显示最大页数和渲染倍率，时间轴面板改显"页面"标签。
- **采样参数面板**：视频模式下展示策略选择器（固定间隔 / 关键帧 / 场景切换 / 均匀分布）；"展开高级参数"折叠区包含起始/结束时间（毫秒）、场景切换阈值、帧最大边长。
- **SSE 实时进度**：`execute()` 启动后立即订阅 `/api/v1/runs/{id}/events` SSE 流，收到含 `progress` 的事件时实时更新进度条；轮询保留作为 fallback；取消/组件卸载时正确 `abort()`。
- **OCR/人像边界框 Canvas 叠加**：媒体预览区改为 `<canvas>` 覆盖层；每次切换单元时重绘；对不同对象类型使用语义配色（人员、人脸、轮廓、文字、标题、表格等）；包含置信度标签（不透明背景白色文字，字号随画面尺寸自适应）。
- **结果导出**：工具栏新增"导出 JSON"和"导出 CSV"按钮；CSV 包含单元级 + 对象级明细，首字节为 UTF-8 BOM 以保证 Excel 正常打开。
- **告警面板**：有告警时在运行进度条下方展示黄色警示面板，支持折叠/展开，使用 `labelWarning()` 翻译。
- **采样元数据行**：结果摘要的媒体元数据区展示采样策略、已读帧数、重连次数、解码耗时等新字段。

### 控制台媒体管理（MediaView）

- 资产表格顶部新增类型筛选分段控件（全部 / 图片 / 视频 / 文档），统计当前筛选集的资产数量和总大小。
- 每行新增"展开元数据"按钮，点击展开显示尺寸、时长、帧率、编码、页数和 SHA-256 前缀等详细信息。
- 新增行级复选框和工具栏"批量删除"按钮，可一次删除多个资产（单个失败不中断批次）。
- 预览弹窗支持视频文件内嵌播放（`<video>` 元素），图片和视频走不同分支。

### 控制台运行管理（RunsView）

- 汇总统计卡展示进行中、已完成、失败的运行数量。
- 每行新增进度条和耗时列。
- 新增"自动刷新"复选框：仅在有活跃运行时以 3 秒间隔轮询，无活跃运行时停止计时，组件卸载时清理定时器。
- 结果弹窗工具栏新增"下载 JSON"按钮。

### 前端标签层

- `labels.ts` 新增 `sampleStrategyLabels` 及 `labelSampleStrategy()` 导出函数。
- `terminationReasonLabels` 新增 `sample_window_completed` 中文映射。
- `types.ts` 中 `MediaTechnicalMetadata` 同步新增 11 个可选字段；`SampleStrategy` 类型导出；`OcrBlock` 补充 `polygon` 字段。

### 媒体采样测试

- 新增 `tests/test_media_sampling.py`，覆盖：固定间隔、均匀分布、关键帧、场景切换策略，时间窗口 + seek，seek 不可用时的跳帧回退，帧缩放，图片缩放，流重连次数上报，不合法策略名称拒绝，SamplePlan 参数校验。
- 全量 Python 回归：**115 passed, 6 skipped**，覆盖率 **78.45%**（门禁 75%）。
- `npm run check`：ESLint、7 项 Vitest 单测、Vue 类型检查、控制台构建、TypeScript SDK 检查均通过。
- `python scripts/repository_gate.py` 和 `python scripts/release_gate.py --implementation-only` 均通过。

## 2026-07-30：0.3.0-dev.0 产品矩阵与统一访问底座

本版本把 Scenara 从单一视觉解析产品推进为可演进的产品矩阵底座。它是向后兼容的开发快照，不是正式 `1.0.0` 生产发行版；合法模型、固定评估集、目标 GPU、隔离网络、离线安装、恢复演练、软件许可证法务批准和具名签署证据仍由严格发布门禁失败关闭。

### 产品矩阵与战略边界

- 确立 Scenara 平台母品牌和 11 项统一目录：Scenara Parse、Model、Data、Edge、Flow、Search、Agent、Console、API、SDK 与 Index。
- 将产品区分为产品模块、共享控制面、开发者平台和底层底座，并以 `available`、`seed`、`planned`、`gated` 明确成熟度，避免把远期名称误写为当前生产能力。
- 新增 `GET /api/v1/platform/products` 产品目录契约，返回每个产品的层级、成熟度、当前范围、非目标、Console 路由、API 路径、依赖关系和下一道门禁。
- 明确近期仍采用模块化单体和共享 Console/API/SDK，不为产品名称复制后端、身份系统、控制台或部署栈；Index 稳定后再支撑 Search，Flow 与 Search 稳定后再启动 Agent，Edge 等待 1.0 服务端交付闭环。

### IAM、认证与产品授权

- 建立共享 IAM 一等资源：Organization、Project、User、Role、Membership、Service Account、API Key 和 Product Entitlement，并提供库存汇总与访问底座状态接口。
- 新增组织、项目、用户、角色、项目成员、服务账号、API Key 和项目产品授权的 `/api/v1/platform/*` 管理接口；所有写操作进入平台审计日志。
- 保留平台根令牌用于首次引导和平台级管理，同时新增绑定租户、项目和服务账号的 API Key；密钥只在签发响应中返回一次，服务端仅保存 SHA-256 摘要与非敏感前缀。
- API Key 的 scope 和产品范围不得超过所属服务账号；撤销、过期、账号停用、租户或项目不匹配都会拒绝认证，成功使用会更新 `last_used_at`。
- scope 统一采用 `resource:action`，支持 `resource:*` 与 `*`；服务凭据有效产品集合按“服务账号产品 ∩ API Key 产品 ∩ 项目启用授权”计算。
- 媒体、运行、流水线、人像、反馈、难例、模型、Webhook、企业工作区与 IAM 已接入共享产品策略映射；暂停 Product Entitlement 会立即收回对应产品访问，恢复后无需重新签发 Key。

### 持久化、Console 与开发者契约

- 新增内存与 PostgreSQL IAM 仓库；`migrations/0002_access_foundation.sql` 创建八类访问底座表、唯一约束、外键与索引，并保持现有 `0001_initial.sql` 增量升级路径。
- Console 总览新增完整产品矩阵；“接入”工作区新增组织项目、用户角色、项目成员、服务账号、API Key 和产品授权管理页签，以及只展示一次的凭据签发对话框。
- Python SDK 与 TypeScript SDK 新增产品目录、访问底座、IAM 资源、API Key 和 Product Entitlement 的强类型方法；OpenAPI 与 TypeScript 生成契约同步更新。
- 新增产品矩阵和访问底座战略文档，并将当前已落地边界、长期缺口和推荐演进顺序写入发布实施矩阵。

### Console 全面中文化

- 新增根目录 `DESIGN.md`，把中文优先、允许保留的专有名称、统一术语、异常兜底、响应式和可访问性要求固化为长期界面契约。
- 产品矩阵和访问底座不再直接展示后端英文名称、说明与下一阶段门禁；11 个产品、7 类访问能力、策略提供者、授权来源和成熟度统一经过中文标签层展示。
- 将通用界面中的 `Scope`、`Key` 和 `ID` 分别统一为“权限范围”“密钥”和“标识”；API、OCR、HTTP(S)、JSON、SHA-256、GPU、PostgreSQL、Redis、S3、MiB 等专有名称和标准单位保持原样。
- 运行错误、媒体终止原因、结果告警、模型部署原因和未知前端异常统一使用中文映射或中文兜底，不再直接把后端英文或浏览器英文异常暴露给用户。
- 运行版本 `0.3.0.dev0` 在界面显示为“0.3.0 开发版”，默认租户和项目显示为“默认租户 / 默认项目”，技术契约值保持不变。
- Vitest 增加通用英文标签回归清单；Playwright 在 12 个工作区、全部接入标签页及桌面/移动视口扫描实际可见文本，只允许品牌、协议、算法、运行库和标准单位。

### 品牌视觉系统

- 采用蓝紫青双曲面带与中心四向星作为 Scenara 景枢标准品牌标志，并将“连接视觉 · 理解世界”登记为品牌口号。
- 同步更新标准全彩、深色背景、单色、应用图标、横版和竖版字标资产；Console 侧栏、移动端品牌区和浏览器图标使用同一几何母体。
- 品牌规范明确标志色阶、渐变、留白、最小尺寸和禁用规则；界面语义色保持原有克制体系，不将 Logo 的蓝紫渐变扩散为单色产品主题。

### 模型运行闭环与正式跨仓库契约

- 新增 `POST /api/v1/model-packages/admissions`，只接受 `schema_version: 1.0` 的正式模型清单；模型制品 URI 的 SHA-256 必须与清单摘要一致，模型卡和评估证据也必须使用不可变摘要引用。
- `ModelRelease` 固化能力和 `runtime_model_id`。激活/回滚按租户、项目和能力维护唯一 active 版本；`0003_model_runtime_contracts.sql` 增加能力列并升级 PostgreSQL 唯一索引。
- Run 开始执行时解析并冻结当前 active 绑定，通过任务级 ContextVar 传入现有 Portrait ONNX 运行时；运行时按精确配置标识、版本和制品 SHA-256 加载，Result 来源记录正式模型标识与版本。
- 部署状态变化生成扩展后的 `ModelDeploymentEvent`，同时进入 `model.deployment.changed` Webhook Outbox；训练仓库可获得签名投递、重试、死信和可查询回执。
- 四条跨仓库契约发布为 `@scenara/repository-contracts@1.0.0`，每条包含 JSON Schema Draft 2020-12、有效示例和摘要；发布索引锁定 `manifest.json`，已发布目录禁止原地修改。
- 新增 `GET /api/v1/platform/contracts` 与单契约 Schema 下载接口；Python/TypeScript SDK 增加契约目录查询和模型包准入方法。
- 新增契约 CLI，支持生成、漂移校验、生产方文档验证、相对上一版本的消费方兼容检查和确定性 ZIP 打包；CI 发布 `repository-contracts-1.0.0` 制品。

### 版本、兼容性与验证

- Python 主包与 Python SDK 升级为 `0.3.0.dev0`；npm 根包、Console、TypeScript SDK、默认容器镜像和离线包标签升级为 `0.3.0-dev.0`。
- OpenAPI `info.version`、`/livez`、系统状态、Python SDK User-Agent、部署示例和 Console E2E mock 统一使用新版本。
- 公共 API 本次只新增契约，没有移除已有 `/api/v1` 路径；PostgreSQL 部署升级时必须先执行 `0002_access_foundation.sql`。
- 本轮回归共 97 项 Python 测试通过、6 项真实服务集成测试在默认运行中跳过，覆盖率为 79.05%；启用 Docker PostgreSQL/pgvector、Redis 与 MinIO 后 6 项集成测试全部通过。Console 桌面与移动端 30 项 Playwright 验收通过，Ruff、Mypy、前端检查、SDK 构建、OpenAPI/SDK 漂移检查、契约门禁、仓库门禁和实现门禁均通过。

### 仍未完成的长期工作

- 交互式用户会话、OIDC、SAML、SCIM，以及登录时根据 Membership/Role 生成有效权限；
- 配额、套餐、席位、计量、账单、自助购买和完整商业授权生命周期；
- 用户、项目和服务账号的停用、删除、恢复、审批，以及集中审计检索、导出与保留策略；
- Scenara Model 的训练作业与实验跟踪、Scenara Data 的完整数据治理、Scenara Edge 的设备和离线同步；
- Scenara Index 的通用索引资源、Scenara Search、Flow 与 Agent 的生产闭环；
- 正式 `1.0.0` 所要求的全部目标环境和具名签署证据。

## 2026-07-30：0.2.0-dev.0 发布工程与运行可靠性加固

- 启用新的开发版本号：Python 主包与 Python SDK 为 `0.2.0.dev0`，npm 根包、中文控制台和 TypeScript SDK 为 `0.2.0-dev.0`；OpenAPI `info.version`、系统状态、健康接口、SDK User-Agent、部署示例和默认镜像 tag 同步到新版本。
- 生产 Compose 与离线包默认镜像 tag 从容易误导的 `1.0.0` 调整为 `0.2.0-dev.0`；正式 `1.0.0` 仍只能在九类证据和软件许可证法务批准全部通过严格发布门禁后启用。
- 严格发布清单升级为 1.1：九类报告必须绑定同一源提交、应用镜像 digest、离线包、OpenAPI 和模型集合 SHA-256；新增软件许可证法务批准证据，工程占位许可证无法通过正式发布门禁。
- 离线构建自动输出模型文件摘要清单、容器镜像身份清单和配套 `release-identity.json`，减少人工转录发布制品标识的错误。
- 新增 `/livez`、依赖感知 `/readyz` 和受认证保护的 Prometheus `/metrics`；请求量按方法、路由和状态计数，耗时以直方图输出，可计算 p95/p99。
- PostgreSQL、对象存储和 Redis 适配器实现真实健康检查；API、Worker 和 Scheduler 生命周期增加回归测试，Legacy Portrait 实际桥接路径纳入测试。
- 迁移任务改为按版本增量执行，并要求每个迁移在自身事务内登记版本；补充升级、恢复式回滚、探针、告警与已知限制运维基线。
- 生产 Python 依赖统一为带哈希的 `requirements/production.lock`，固定 uv 为 Python 3.12、x86_64 manylinux 目标解析；Docker、离线轮子、集成测试和供应链作业使用同一锁文件，CI 重新解析并阻止漂移。运行时依赖中移除审计工具和未启用的 OpenTelemetry 包。
- PostgreSQL/pgvector、Redis、MinIO 和 MinIO Client 镜像固定到已验证的 manifest digest；严格发布身份继续绑定最终应用镜像 digest。
- CI 将 `app/` 迁移适配层纳入 Ruff 正确性规则和 Mypy，并启用 75% Python 覆盖率下限；当前 81 项本地测试通过，覆盖率 77.12%。
- 新增 Playwright 桌面 Chrome 与 Pixel 7 验收，12 条工作区路由均检查标题、主标题、页面异常和横向溢出，移动导航另行验证。
- API、OpenAPI 和系统状态统一使用 Python 包版本，避免手写版本漂移；前端桌面/移动 E2E 的版本 mock 已同步到 `0.2.0.dev0`。

## 2026-07-30：持续集成兼容性修复

- 修复 GitHub Actions 真实服务集成与备份恢复演练在解析生产 Compose 时缺少 `SCENARA_OCR_ENGINE_FACTORY` 的问题；测试作业仅提供明确的未使用适配器占位值，生产配置仍保持必填和失败关闭。
- 移除 Redis 与 PyJWT 可选依赖在“已安装”和“未安装”两种环境下表现不一致的 `type: ignore`，改用显式动态导入，使严格 Mypy 检查在干净的 CI 环境与完整生产依赖环境中均可通过。
- 将 `actions/checkout`、`actions/setup-python`、`actions/setup-node`、`actions/upload-artifact` 与 `pnpm/action-setup` 升级到支持 Node.js 24 运行时的主版本，消除 GitHub 托管运行器的 Node.js 20 弃用警告。
- 新增部署工作流契约测试，持续校验所有使用生产 Compose 的 CI 作业都提供必填变量，并阻止上述官方 Actions 回退到不兼容的旧主版本。
- 修复备份与恢复脚本在 Linux 中直接执行无执行位校验脚本导致的退出码 126；统一经由 `bash` 调用，使 Git 检出和离线介质未保留执行位时仍能完成校验。
- 统一真实服务生命周期测试与其他 MinIO 集成测试的密钥来源，使用工作流注入的临时密钥，修复随机化凭据后 `HeadBucket` 返回 403 的问题。

## 2026-07-30：全面优化升级方案落地

本次更新对照《Scenara 景枢全面优化升级方案.md》完成仓库内可实施的 Phase 0 至 Phase 5 开发工作。代码、自动化、模板与本机可执行验收均已落地；需要合法模型、固定评估集、目标 NVIDIA GPU、隔离网络和具名签署人的生产资格证据继续由严格发布门禁失败关闭，不以模板或开发机结果冒充正式证据。

### Phase 0：基线与品牌收口

- 中文品牌统一为“景枢”，完整名称统一为“Scenara 景枢”；保留 `Scenara/scenara` 技术标识、`/api/v1`、SDK 包名、镜像名、数据库表前缀和 `SCENARA_*` 环境变量。
- 新增品牌决策 ADR、品牌规范、横版与竖版字标、应用图标、单色与反色标志；旧中文品牌仅保留在历史迁移说明中。
- 更新 README、部署材料、OpenAPI、SDK 文档、示例客户端、模型配置和控制台品牌文案。
- 建立仓库门禁，检查遗留品牌、敏感信息、模型资产政策、许可证、来源声明、架构边界和生成制品漂移。
- 建立 GitHub Actions：Python、前端、TypeScript SDK、真实服务集成、依赖许可清单、SBOM、密钥扫描、安全测试和备份恢复演练。
- CI 新增全部部署 Shell 脚本语法校验和控制台容器构建阶段，确保 `/console/` 生产静态资源可由镜像复现。
- 固定 Ubuntu、Docker Compose、NVIDIA GPU、PostgreSQL/pgvector、Redis、MinIO、Python、Node.js 与浏览器支持矩阵。

### Phase 1：平台服务与安全

- 实现 PostgreSQL 事实存储、Redis 至少一次投递队列、S3/MinIO 对象存储、pgvector 特征存储及对应生命周期管理。
- 完成媒体、运行、不可变结果、流水线版本、模型包、Webhook、审计、留存、对象删除和身份数据删除契约。
- 实现幂等键、乐观并发、租约恢复、重复投递去重、SSE 事件续传、Webhook 签名/退避/死信/重放和事件序号恢复。
- 实现 HTTP(S)/RTSP/RTMP 源地址 SSRF 策略、凭据加密与脱敏、图片/PDF 媒体限制、解压炸弹防护、授权隔离和审计失败关闭。
- 默认留存策略固定为原始媒体 7 天、预览 30 天、结构化结果 180 天。
- 新增 `deploy/compose.integration.yml` 和真实服务集成测试；本机 Docker PostgreSQL/pgvector、Redis、MinIO 测试结果为 5 passed、0 skipped。

### Phase 2：模型与领域治理

- 新增模型包清单，包含模型标识、版本、能力、适配器、权重 SHA-256、来源、许可证、模型卡、显存预算、回归样例和生产审批状态。
- 生产配置缺少必需模型、模型包校验失败或仍使用开发适配器时明确失败，不静默回退。
- 人像领域补齐检测、身体重识别、人脸检测/对齐/特征、姿态、人体解析、服饰属性、服饰分割、步态、轨迹和质量融合契约。
- OCR/Document 补齐文字检测与识别、中文文本、旋转处理、PDF 页面、阅读顺序、标题、段落、图片区域和表格版面契约。
- 能力矩阵使用 Contracted、Implemented、Qualified、Released 分级，不以代码存在代替模型质量与生产资格结论。
- 建立九类发布证据模板、责任人与签署角色；软件许可证、模型权属、人像评估和 OCR 评估在缺少法务或具名批准时保持未通过。

### Phase 3：私有化交付

- 完成单节点 Docker Compose 编排，包含 API、批处理/实时 GPU Worker、调度器、PostgreSQL/pgvector、Redis、MinIO 与迁移任务。
- 生产环境要求认证、加密密钥、生产模型和显式凭据，缺失时失败关闭。
- 新增离线包构建与安装、镜像/配置/模型 SHA-256 校验、升级/回滚说明、运维手册、备份、恢复与备份校验脚本。
- 离线包现在强制包含已批准模型目录、示例和两套 SDK；安装程序校验 Ubuntu 24.04、x86_64、Docker/Compose 版本、CUDA 兼容性和不少于 23,000 MiB 的单卡显存，并检查接口、中文控制台和核心服务健康状态。
- 生产镜像使用 Node 22.22.2 多阶段构建控制台并交由 FastAPI 同源提供 `/console/`、静态资源和单页路由回退，根路径自动进入中文控制台并设置安全响应头。
- 本机执行 PostgreSQL + MinIO 备份、破坏、恢复和标记校验演练并通过；恢复后再次运行真实服务集成测试，仍为 5 passed。
- `scripts/release_gate.py --implementation-only` 已通过；严格发布门禁在软件许可证未获法务批准或缺少九类签署报告时按设计拒绝发布。
- 严格门禁现在要求九类证据各且仅各一份，核验发布身份、报告路径、内容 SHA-256、时区时间戳、执行与批准顺序、签名者、目标环境和分类元数据；空白、重复、占位、跳过或开发机替代报告均会被拒绝。
- 正式 1.0 仍需在支持矩阵目标机完成不少于 23,000 MiB 单 GPU 容量、隔离网络离线安装、合法模型评估与具名签署。

### Phase 4：反馈与模型发布闭环

- 新增版本化 Feedback/Review 契约，覆盖误检、漏检、属性错误、身份匹配错误和 OCR 更正，并记录租户、项目、媒体、Run、Result、Pipeline 与模型来源。
- 反馈提交只接受已完成运行和结果中的模型标识；服务端读取不可变结果并校验对象摘要、租户/项目、流水线、媒体和模型来源，拒绝调用方伪造追溯引用及包含原始敏感数据的更正内容。
- 新增难例清单，只导出对象引用、修正标签、模型/流水线版本和合规状态；未授权、未脱敏或未批准反馈无法导出。
- 难例清单新增固定标签规范和训练/验证/测试用途；反馈、清单、发布与部署事件统一携带 `schema_version: 1.0`。
- 新增模型发布状态机 `candidate -> validated -> approved -> active -> retired`，激活前要求生产模型包和证据引用。
- 新增唯一活动版本约束、部署事件审计、上一生产版本保留和受控回滚。
- 完成内存与 PostgreSQL 适配器、公共 API、Python SDK、TypeScript SDK、OpenAPI 生成类型和租户隔离测试。
- 控制台新增反馈审核、难例清单、模型证据、发布审批、部署事件与回滚操作，不引入通用拖拽标注工具。
- 模型发布从候选进入验证、批准或激活前必须登记非空、去重的证据引用；激活仍要求通过摘要校验且标记生产就绪的模型包。

### Phase 5：多领域扩展门禁

- 保持 DomainPlugin、类型化 Result 和现有 Media/Run/Pipeline 契约作为唯一扩展入口，平台内核不增加领域专用分支。
- 新领域立项必须同时具备至少两个付费或联合验证场景、合法模型与固定评估数据、明确负责人、运维预算和支持边界。
- 当前未满足外部商业与模型数据触发条件，因此未虚构 Vehicle、Industrial 或 Behavior 的生产能力；立项门禁已经写入方案、架构约束和实施矩阵。

### 全中文控制台

- 12 条核心路由全部使用中文导航、标题、状态、按钮、表单、空状态、错误提示和治理文案。
- 将通用界面词 `API`、`Bearer Token`、`Webhook`、`SLA`、`ID` 和 `URL` 分别显示为“接口”“访问令牌”“事件回调”“服务等级协议”“标识”和“地址”。
- 保留品牌、行业缩写、协议、单位和不可变技术标识，例如 Scenara、OCR、HTTP(S)、SHA-256、JSON、MiB、模型标识和流水线标识。
- 新增中文文案回归测试，阻止已清理的通用英文标签重新进入 Vue 界面。
- 领域能力、流水线、处理节点、结果对象、运行状态和未知枚举均使用中文映射；后端错误码和网络异常也统一显示中文提示。
- 修复移动端网格面板被宽表格撑开的问题，并为媒体源表格增加容器内横向滚动；12 条路由在 390×844/375×844 视口横向溢出均为 0。

### SDK、契约与示例

- Python SDK 与 TypeScript SDK 覆盖媒体、运行、结果分页、Webhook、反馈、难例清单、模型发布和企业工作区接口。
- TypeScript 模型从 `docs/openapi.json` 生成，并通过零漂移检查；发布构建同步更新 `sdk/typescript/dist`。
- Python 与 Node.js 示例客户端改为公共 `/api/v1` 契约，包含项目上下文、幂等键和异步运行轮询。

### 验证记录

| 验证项 | 结果 |
|---|---|
| `python -m pytest -q --cov=scenara --cov=sdk/python/scenara_sdk --cov-fail-under=75` | 81 passed，5 个真实服务集成用例按默认配置跳过，77.12% coverage |
| `SCENARA_RUN_INTEGRATION=1 python -m pytest -q -m integration tests/integration` | 5 passed，0 skipped |
| PostgreSQL + MinIO 本地备份恢复演练 | passed，恢复后集成测试再次通过 |
| `npm run check` | passed：ESLint、6 个控制台测试、Vue 类型检查、控制台构建、TypeScript SDK 检查 |
| Ruff 与 Mypy | passed |
| OpenAPI 与 TypeScript SDK 生成漂移检查 | passed |
| `python scripts/repository_gate.py` | passed |
| `python scripts/release_gate.py --implementation-only` | passed |
| 严格 `python scripts/release_gate.py` | 按设计失败关闭：许可证仍为工程占位文本，且尚无正式 `manifest.json` 与九类签署报告 |
| `python -m pip_audit -r requirements/dev.txt` | 未发现已知漏洞 |
| `pnpm audit --audit-level high` | 未发现已知漏洞；锁文件将 `brace-expansion` 固定到已修复的 5.0.8 |
| `bash -n deploy/scripts/*.sh` | passed；使用缓存的 Linux 容器执行 |
| 本机控制台镜像阶段 | 未执行完成：Docker Desktop 代理 `127.0.0.1:10808` 无法拉取官方 Node 基础镜像；同一构建已加入 GitHub CI |
| 浏览器桌面端验收 | 12 条路由连接后端，无 HTTP 错误、无控制台错误、无页面横向溢出 |
| 浏览器移动端验收 | 12 条路由连接后端，无 HTTP 错误、无控制台错误、横向溢出均为 0 |

### 执行环境与追溯

- 基线提交：`ab5d7deac90187b1714bb13954a6d9662e2040e1`。
- 本机环境：Windows 11 build 26200、Python 3.12.3、Node.js v24.13.1、npm 11.8.0、pnpm 11.9.0、Docker Engine 29.1.3、Docker Compose 5.0.0。
- 本机 Node.js 版本高于正式支持的 22.22.2，pnpm 引擎检查会提示警告；正式 CI 与发布验收必须使用支持矩阵版本。
- `pnpm-lock.yaml` SHA-256：`50308c9fd432c9147d4f5e3efe4ea889262a4f633c78a25cf01d4baa540a2e07`。
- `requirements/production.lock` SHA-256：`a44e7e836e1effe09bdcbfa75e9c7c19c1bbd2acd09186fa37e73d4dbfbefda4`。
- `requirements-cpu.lock` SHA-256：`5dab4913da174b3d936d8dfc3f1660882d1b96b7b5e60128e93c6ec5698e4777`。
- Python `pip-audit` 与项目采用的 pnpm 锁文件审计均未发现已知漏洞。

### 发布边界

本次更新完成的是升级方案中当前仓库可实施的开发、自动化、模板和本地验证工作，不等同于 Scenara 1.0 已获得生产发布资格。只有 `docs/release/evidence/manifest.json` 聚合的 `backup_restore`、`gpu_capacity`、`integration_services`、`model_rights`、`ocr_evaluation`、`offline_install`、`portrait_evaluation`、`security_assessment` 和 `software_license_approval` 九类报告全部绑定同一发布身份，由责任人执行、校验和签署，并通过严格发布门禁后，才能将对应能力标记为 Qualified 或 Released。
## 2026-07-30：补充验证与遗留说明

- Python 覆盖率门禁复验通过，结果为 `81 passed, 5 skipped, 77.12% coverage`。
- `SCENARA_RUN_INTEGRATION=1 python -m pytest -q -m integration tests/integration` 复验通过，结果为 `5 passed`。
- `npm run check` 复验通过，前端构建、单测、类型检查和 TypeScript SDK 检查均通过。
- `python -m ruff check .`、`python -m mypy --explicit-package-bases scenara sdk/python/scenara_sdk scripts`、`python scripts/release_gate.py --implementation-only` 均通过。
- `python -m pip_audit -r requirements/dev.txt` 通过，未发现已知漏洞。
- `pnpm audit --audit-level high` 通过，未发现已知漏洞。
- `npm audit --audit-level high` 在当前 workspace 安装树上仍会报出旧的依赖噪声，因此本次发布结论以 `pnpm audit` 为准。
- 严格 `python scripts/release_gate.py` 仍然按设计保持失败关闭，因为软件许可证法务批准和正式的九类签字证据尚未到位；这属于生产资格边界，不是代码合并错误。
- 生产 Compose 现在对 API、Worker 与调度器统一挂载只读私有模型目录，并强制提供 `SCENARA_OCR_ENGINE_FACTORY`；示例加密密钥改为不可直接使用的生成提示，防止示例值被误用于生产。
- 模型发布证据从“非空引用”升级为对象存储实体验证：校验租户/项目作用域、对象 SHA-256、模型标识、版本、模型包摘要、证据类型、签署人、时区时间和分类明细；权属、领域评估与回归证据缺失时禁止状态迁移和回滚。
- 仓库门禁扩大到环境变量示例、Dockerfile、Shell 与 PowerShell 脚本，识别令牌、加密密钥和已知全零 Fernet 密钥，同时正确处理空值与明确占位值；新增对应回归测试。
- 明确发布证据的信任边界：仓库门禁验证报告摘要、结构一致性和具名审批，不冒充密码学身份认证；审批身份及外部签名链由受控发布流程验证和留存。

## 2026-07-30：产品矩阵仓库拓扑落地

- 正式确定当前 `scenara` 仓库为平台集成仓库，不改名为 `scenara-parse`；统一承载 Scenara Parse、Console、API、SDK、共享 IAM/授权/审计、平台运行时以及 Model/Data 的平台侧集成治理。
- 将已有模型训练仓库定义为逻辑标识 `scenara-model` 的专业仓库，负责训练作业、实验跟踪、训练算力调度、训练评估和不可变模型制品；模型准入、发布、激活、回滚和部署审计继续由当前仓库负责。
- 将 `scenara-data` 定义为规划中的专业仓库；在数据集版本、标注审核、质量、血缘、授权、导出和负责人形成稳定边界前不创建空壳仓库，业务媒体、结果、反馈和难例导出继续留在当前平台。
- 新增 `GET /api/v1/platform/repositories` 仓库拓扑契约，公开仓库生命周期、负责与排除职责、四类跨仓库契约以及禁止共享数据库、禁止跨仓库源码导入、仅使用版本化契约和不可变制品引用四项强制规则。
- Python SDK 增加 `get_repository_topology()`，TypeScript SDK 增加 `getRepositoryTopology()`；OpenAPI、生成类型和发布构建制品同步更新。
- Console 总览增加全中文仓库拓扑视图，展示当前仓库、已有 Model 仓库和规划 Data 仓库的职责、非职责、下一道门禁、跨仓库契约与强制边界，并新增英文后端文案泄漏回归测试。
- 新增 `docs/strategy/REPOSITORY_TOPOLOGY.md`，补齐职责矩阵、集成流程、Data 拆分门禁和变更要求；产品矩阵、README、发布说明与实施矩阵同步更新。
- 修复密钥扫描将公开的 `OPENAPI_SHA256` 契约摘要识别为泄漏的问题：生成器仅在该公开摘要常量同行写入 `gitleaks:allow`，未增加文件级或规则级宽泛白名单。
