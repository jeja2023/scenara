# Scenara 景枢更新日志

## 2026-07-30：0.3.0-dev.0 产品矩阵与统一访问底座

本版本把 Scenara 从单一视觉解析产品推进为可演进的产品矩阵底座。它是向后兼容的开发快照，不是正式 `1.0.0` 生产发行版；合法模型、固定评估集、目标 GPU、隔离网络、离线安装、恢复演练、软件许可证法务批准和具名签署证据仍由严格发布门禁失败关闭。

### 产品矩阵与战略边界

- 确立 Scenara 平台母品牌和 11 项统一目录：Scenara Parse、Model、Data、Edge、Flow、Search、Agent、Console、API、SDK 与 Index。
- 将产品区分为产品模块、共享控制面、开发者平台和底层底座，并以 `available`、`seed`、`planned`、`gated` 明确成熟度，避免把远期名称误写为当前生产能力。
- 新增 `GET /api/v1/platform/products` 产品目录契约，返回每个产品的层级、成熟度、当前范围、非目标、Console 路由、API 路径、依赖关系和下一道门禁。
- 明确近期仍采用模块化单体和共享 Console/API/SDK，不为产品名称复制后端、身份系统、控制台或部署栈；Index 稳定后再支撑 Search，Flow 与 Search 稳定后再启动 Agent，Edge 等待 1.0 服务端交付闭环。

### IAM、认证与产品授权

- 建立共享 IAM 一等资源：Organization、Project、User、Role、Membership、Service Account、API Key 和 Product Entitlement，并提供库存汇总与访问底座状态接口。
- 新增组织、项目、用户、角色、项目成员、服务账号、API Key 和项目产品授权的 `/api/v1/platform/*` 管理接口；所有写操作进入平台审计日志。
- 保留平台根令牌用于首次引导和平台级管理，同时新增绑定租户、项目和服务账号的 API Key；密钥只在签发响应中返回一次，服务端仅保存 SHA-256 摘要与非敏感前缀。
- API Key 的 scope 和产品范围不得超过所属服务账号；撤销、过期、账号停用、租户或项目不匹配都会拒绝认证，成功使用会更新 `last_used_at`。
- scope 统一采用 `resource:action`，支持 `resource:*` 与 `*`；服务凭据有效产品集合按“服务账号产品 ∩ API Key 产品 ∩ 项目启用授权”计算。
- 媒体、运行、流水线、人像、反馈、难例、模型、Webhook、企业工作区与 IAM 已接入共享产品策略映射；暂停 Product Entitlement 会立即收回对应产品访问，恢复后无需重新签发 Key。

### 持久化、Console 与开发者契约

- 新增内存与 PostgreSQL IAM 仓库；`migrations/0002_access_foundation.sql` 创建八类访问底座表、唯一约束、外键与索引，并保持现有 `0001_initial.sql` 增量升级路径。
- Console 总览新增完整产品矩阵；“接入”工作区新增组织项目、用户角色、项目成员、服务账号、API Key 和产品授权管理页签，以及只展示一次的凭据签发对话框。
- Python SDK 与 TypeScript SDK 新增产品目录、访问底座、IAM 资源、API Key 和 Product Entitlement 的强类型方法；OpenAPI 与 TypeScript 生成契约同步更新。
- 新增产品矩阵和访问底座战略文档，并将当前已落地边界、长期缺口和推荐演进顺序写入发布实施矩阵。

### Console 全面中文化

- 新增根目录 `DESIGN.md`，把中文优先、允许保留的专有名称、统一术语、异常兜底、响应式和可访问性要求固化为长期界面契约。
- 产品矩阵和访问底座不再直接展示后端英文名称、说明与下一阶段门禁；11 个产品、7 类访问能力、策略提供者、授权来源和成熟度统一经过中文标签层展示。
- 将通用界面中的 `Scope`、`Key` 和 `ID` 分别统一为“权限范围”“密钥”和“标识”；API、OCR、HTTP(S)、JSON、SHA-256、GPU、PostgreSQL、Redis、S3、MiB 等专有名称和标准单位保持原样。
- 运行错误、媒体终止原因、结果告警、模型部署原因和未知前端异常统一使用中文映射或中文兜底，不再直接把后端英文或浏览器英文异常暴露给用户。
- 运行版本 `0.3.0.dev0` 在界面显示为“0.3.0 开发版”，默认租户和项目显示为“默认租户 / 默认项目”，技术契约值保持不变。
- Vitest 增加通用英文标签回归清单；Playwright 在 12 个工作区、全部接入标签页及桌面/移动视口扫描实际可见文本，只允许品牌、协议、算法、运行库和标准单位。

### 品牌视觉系统

- 采用蓝紫青双曲面带与中心四向星作为 Scenara 景枢标准品牌标志，并将“连接视觉 · 理解世界”登记为品牌口号。
- 同步更新标准全彩、深色背景、单色、应用图标、横版和竖版字标资产；Console 侧栏、移动端品牌区和浏览器图标使用同一几何母体。
- 品牌规范明确标志色阶、渐变、留白、最小尺寸和禁用规则；界面语义色保持原有克制体系，不将 Logo 的蓝紫渐变扩散为单色产品主题。

### 版本、兼容性与验证

- Python 主包与 Python SDK 升级为 `0.3.0.dev0`；npm 根包、Console、TypeScript SDK、默认容器镜像和离线包标签升级为 `0.3.0-dev.0`。
- OpenAPI `info.version`、`/livez`、系统状态、Python SDK User-Agent、部署示例和 Console E2E mock 统一使用新版本。
- 公共 API 本次只新增契约，没有移除已有 `/api/v1` 路径；PostgreSQL 部署升级时必须先执行 `0002_access_foundation.sql`。
- 本轮回归共 87 项 Python 测试通过、6 项真实服务集成测试在默认运行中跳过，覆盖率为 78.29%；启用 Docker PostgreSQL/pgvector、Redis 与 MinIO 后 6 项集成测试全部通过。Console 桌面与移动端 28 项 Playwright 验收通过，Ruff、Mypy、前端检查、SDK 构建、OpenAPI/SDK 漂移检查、仓库门禁和实现门禁均通过。

### 仍未完成的长期工作

- 交互式用户会话、OIDC、SAML、SCIM，以及登录时根据 Membership/Role 生成有效权限；
- 配额、套餐、席位、计量、账单、自助购买和完整商业授权生命周期；
- 用户、项目和服务账号的停用、删除、恢复、审批，以及集中审计检索、导出与保留策略；
- Scenara Model 的训练作业与实验跟踪、Scenara Data 的完整数据治理、Scenara Edge 的设备和离线同步；
- Scenara Index 的通用索引资源、Scenara Search、Flow 与 Agent 的生产闭环；
- 正式 `1.0.0` 所要求的全部目标环境和具名签署证据。

## 2026-07-30：0.2.0-dev.0 发布工程与运行可靠性加固

- 启用新的开发版本号：Python 主包与 Python SDK 为 `0.2.0.dev0`，npm 根包、中文控制台和 TypeScript SDK 为 `0.2.0-dev.0`；OpenAPI `info.version`、系统状态、健康接口、SDK User-Agent、部署示例和默认镜像 tag 同步到新版本。
- 生产 Compose 与离线包默认镜像 tag 从容易误导的 `1.0.0` 调整为 `0.2.0-dev.0`；正式 `1.0.0` 仍只能在九类证据和软件许可证法务批准全部通过严格发布门禁后启用。
- 严格发布清单升级为 1.1：九类报告必须绑定同一源提交、应用镜像 digest、离线包、OpenAPI 和模型集合 SHA-256；新增软件许可证法务批准证据，工程占位许可证无法通过正式发布门禁。
- 离线构建自动输出模型文件摘要清单、容器镜像身份清单和配套 `release-identity.json`，减少人工转录发布制品标识的错误。
- 新增 `/livez`、依赖感知 `/readyz` 和受认证保护的 Prometheus `/metrics`；请求量按方法、路由和状态计数，耗时以直方图输出，可计算 p95/p99。
- PostgreSQL、对象存储和 Redis 适配器实现真实健康检查；API、Worker 和 Scheduler 生命周期增加回归测试，Legacy Portrait 实际桥接路径纳入测试。
- 迁移任务改为按版本增量执行，并要求每个迁移在自身事务内登记版本；补充升级、恢复式回滚、探针、告警与已知限制运维基线。
- 生产 Python 依赖统一为带哈希的 `requirements/production.lock`，固定 uv 为 Python 3.12、x86_64 manylinux 目标解析；Docker、离线轮子、集成测试和供应链作业使用同一锁文件，CI 重新解析并阻止漂移。运行时依赖中移除审计工具和未启用的 OpenTelemetry 包。
- PostgreSQL/pgvector、Redis、MinIO 和 MinIO Client 镜像固定到已验证的 manifest digest；严格发布身份继续绑定最终应用镜像 digest。
- CI 将 `app/` 迁移适配层纳入 Ruff 正确性规则和 Mypy，并启用 75% Python 覆盖率下限；当前 81 项本地测试通过，覆盖率 77.12%。
- 新增 Playwright 桌面 Chrome 与 Pixel 7 验收，12 条工作区路由均检查标题、主标题、页面异常和横向溢出，移动导航另行验证。
- API、OpenAPI 和系统状态统一使用 Python 包版本，避免手写版本漂移；前端桌面/移动 E2E 的版本 mock 已同步到 `0.2.0.dev0`。

## 2026-07-30：持续集成兼容性修复

- 修复 GitHub Actions 真实服务集成与备份恢复演练在解析生产 Compose 时缺少 `SCENARA_OCR_ENGINE_FACTORY` 的问题；测试作业仅提供明确的未使用适配器占位值，生产配置仍保持必填和失败关闭。
- 移除 Redis 与 PyJWT 可选依赖在“已安装”和“未安装”两种环境下表现不一致的 `type: ignore`，改用显式动态导入，使严格 Mypy 检查在干净的 CI 环境与完整生产依赖环境中均可通过。
- 将 `actions/checkout`、`actions/setup-python`、`actions/setup-node`、`actions/upload-artifact` 与 `pnpm/action-setup` 升级到支持 Node.js 24 运行时的主版本，消除 GitHub 托管运行器的 Node.js 20 弃用警告。
- 新增部署工作流契约测试，持续校验所有使用生产 Compose 的 CI 作业都提供必填变量，并阻止上述官方 Actions 回退到不兼容的旧主版本。
- 修复备份与恢复脚本在 Linux 中直接执行无执行位校验脚本导致的退出码 126；统一经由 `bash` 调用，使 Git 检出和离线介质未保留执行位时仍能完成校验。
- 统一真实服务生命周期测试与其他 MinIO 集成测试的密钥来源，使用工作流注入的临时密钥，修复随机化凭据后 `HeadBucket` 返回 403 的问题。

## 2026-07-30：全面优化升级方案落地

本次更新对照《Scenara 景枢全面优化升级方案.md》完成仓库内可实施的 Phase 0 至 Phase 5 开发工作。代码、自动化、模板与本机可执行验收均已落地；需要合法模型、固定评估集、目标 NVIDIA GPU、隔离网络和具名签署人的生产资格证据继续由严格发布门禁失败关闭，不以模板或开发机结果冒充正式证据。

### Phase 0：基线与品牌收口

- 中文品牌统一为“景枢”，完整名称统一为“Scenara 景枢”；保留 `Scenara/scenara` 技术标识、`/api/v1`、SDK 包名、镜像名、数据库表前缀和 `SCENARA_*` 环境变量。
- 新增品牌决策 ADR、品牌规范、横版与竖版字标、应用图标、单色与反色标志；旧中文品牌仅保留在历史迁移说明中。
- 更新 README、部署材料、OpenAPI、SDK 文档、示例客户端、模型配置和控制台品牌文案。
- 建立仓库门禁，检查遗留品牌、敏感信息、模型资产政策、许可证、来源声明、架构边界和生成制品漂移。
- 建立 GitHub Actions：Python、前端、TypeScript SDK、真实服务集成、依赖许可清单、SBOM、密钥扫描、安全测试和备份恢复演练。
- CI 新增全部部署 Shell 脚本语法校验和控制台容器构建阶段，确保 `/console/` 生产静态资源可由镜像复现。
- 固定 Ubuntu、Docker Compose、NVIDIA GPU、PostgreSQL/pgvector、Redis、MinIO、Python、Node.js 与浏览器支持矩阵。

### Phase 1：平台服务与安全

- 实现 PostgreSQL 事实存储、Redis 至少一次投递队列、S3/MinIO 对象存储、pgvector 特征存储及对应生命周期管理。
- 完成媒体、运行、不可变结果、流水线版本、模型包、Webhook、审计、留存、对象删除和身份数据删除契约。
- 实现幂等键、乐观并发、租约恢复、重复投递去重、SSE 事件续传、Webhook 签名/退避/死信/重放和事件序号恢复。
- 实现 HTTP(S)/RTSP/RTMP 源地址 SSRF 策略、凭据加密与脱敏、图片/PDF 媒体限制、解压炸弹防护、授权隔离和审计失败关闭。
- 默认留存策略固定为原始媒体 7 天、预览 30 天、结构化结果 180 天。
- 新增 `deploy/compose.integration.yml` 和真实服务集成测试；本机 Docker PostgreSQL/pgvector、Redis、MinIO 测试结果为 5 passed、0 skipped。

### Phase 2：模型与领域治理

- 新增模型包清单，包含模型标识、版本、能力、适配器、权重 SHA-256、来源、许可证、模型卡、显存预算、回归样例和生产审批状态。
- 生产配置缺少必需模型、模型包校验失败或仍使用开发适配器时明确失败，不静默回退。
- 人像领域补齐检测、身体重识别、人脸检测/对齐/特征、姿态、人体解析、服饰属性、服饰分割、步态、轨迹和质量融合契约。
- OCR/Document 补齐文字检测与识别、中文文本、旋转处理、PDF 页面、阅读顺序、标题、段落、图片区域和表格版面契约。
- 能力矩阵使用 Contracted、Implemented、Qualified、Released 分级，不以代码存在代替模型质量与生产资格结论。
- 建立九类发布证据模板、责任人与签署角色；软件许可证、模型权属、人像评估和 OCR 评估在缺少法务或具名批准时保持未通过。

### Phase 3：私有化交付

- 完成单节点 Docker Compose 编排，包含 API、批处理/实时 GPU Worker、调度器、PostgreSQL/pgvector、Redis、MinIO 与迁移任务。
- 生产环境要求认证、加密密钥、生产模型和显式凭据，缺失时失败关闭。
- 新增离线包构建与安装、镜像/配置/模型 SHA-256 校验、升级/回滚说明、运维手册、备份、恢复与备份校验脚本。
- 离线包现在强制包含已批准模型目录、示例和两套 SDK；安装程序校验 Ubuntu 24.04、x86_64、Docker/Compose 版本、CUDA 兼容性和不少于 23,000 MiB 的单卡显存，并检查接口、中文控制台和核心服务健康状态。
- 生产镜像使用 Node 22.22.2 多阶段构建控制台并交由 FastAPI 同源提供 `/console/`、静态资源和单页路由回退，根路径自动进入中文控制台并设置安全响应头。
- 本机执行 PostgreSQL + MinIO 备份、破坏、恢复和标记校验演练并通过；恢复后再次运行真实服务集成测试，仍为 5 passed。
- `scripts/release_gate.py --implementation-only` 已通过；严格发布门禁在软件许可证未获法务批准或缺少九类签署报告时按设计拒绝发布。
- 严格门禁现在要求九类证据各且仅各一份，核验发布身份、报告路径、内容 SHA-256、时区时间戳、执行与批准顺序、签名者、目标环境和分类元数据；空白、重复、占位、跳过或开发机替代报告均会被拒绝。
- 正式 1.0 仍需在支持矩阵目标机完成不少于 23,000 MiB 单 GPU 容量、隔离网络离线安装、合法模型评估与具名签署。

### Phase 4：反馈与模型发布闭环

- 新增版本化 Feedback/Review 契约，覆盖误检、漏检、属性错误、身份匹配错误和 OCR 更正，并记录租户、项目、媒体、Run、Result、Pipeline 与模型来源。
- 反馈提交只接受已完成运行和结果中的模型标识；服务端读取不可变结果并校验对象摘要、租户/项目、流水线、媒体和模型来源，拒绝调用方伪造追溯引用及包含原始敏感数据的更正内容。
- 新增难例清单，只导出对象引用、修正标签、模型/流水线版本和合规状态；未授权、未脱敏或未批准反馈无法导出。
- 难例清单新增固定标签规范和训练/验证/测试用途；反馈、清单、发布与部署事件统一携带 `schema_version: 1.0`。
- 新增模型发布状态机 `candidate -> validated -> approved -> active -> retired`，激活前要求生产模型包和证据引用。
- 新增唯一活动版本约束、部署事件审计、上一生产版本保留和受控回滚。
- 完成内存与 PostgreSQL 适配器、公共 API、Python SDK、TypeScript SDK、OpenAPI 生成类型和租户隔离测试。
- 控制台新增反馈审核、难例清单、模型证据、发布审批、部署事件与回滚操作，不引入通用拖拽标注工具。
- 模型发布从候选进入验证、批准或激活前必须登记非空、去重的证据引用；激活仍要求通过摘要校验且标记生产就绪的模型包。

### Phase 5：多领域扩展门禁

- 保持 DomainPlugin、类型化 Result 和现有 Media/Run/Pipeline 契约作为唯一扩展入口，平台内核不增加领域专用分支。
- 新领域立项必须同时具备至少两个付费或联合验证场景、合法模型与固定评估数据、明确负责人、运维预算和支持边界。
- 当前未满足外部商业与模型数据触发条件，因此未虚构 Vehicle、Industrial 或 Behavior 的生产能力；立项门禁已经写入方案、架构约束和实施矩阵。

### 全中文控制台

- 12 条核心路由全部使用中文导航、标题、状态、按钮、表单、空状态、错误提示和治理文案。
- 将通用界面词 `API`、`Bearer Token`、`Webhook`、`SLA`、`ID` 和 `URL` 分别显示为“接口”“访问令牌”“事件回调”“服务等级协议”“标识”和“地址”。
- 保留品牌、行业缩写、协议、单位和不可变技术标识，例如 Scenara、OCR、HTTP(S)、SHA-256、JSON、MiB、模型标识和流水线标识。
- 新增中文文案回归测试，阻止已清理的通用英文标签重新进入 Vue 界面。
- 领域能力、流水线、处理节点、结果对象、运行状态和未知枚举均使用中文映射；后端错误码和网络异常也统一显示中文提示。
- 修复移动端网格面板被宽表格撑开的问题，并为媒体源表格增加容器内横向滚动；12 条路由在 390×844/375×844 视口横向溢出均为 0。

### SDK、契约与示例

- Python SDK 与 TypeScript SDK 覆盖媒体、运行、结果分页、Webhook、反馈、难例清单、模型发布和企业工作区接口。
- TypeScript 模型从 `docs/openapi.json` 生成，并通过零漂移检查；发布构建同步更新 `sdk/typescript/dist`。
- Python 与 Node.js 示例客户端改为公共 `/api/v1` 契约，包含项目上下文、幂等键和异步运行轮询。

### 验证记录

| 验证项 | 结果 |
|---|---|
| `python -m pytest -q --cov=scenara --cov=sdk/python/scenara_sdk --cov-fail-under=75` | 81 passed，5 个真实服务集成用例按默认配置跳过，77.12% coverage |
| `SCENARA_RUN_INTEGRATION=1 python -m pytest -q -m integration tests/integration` | 5 passed，0 skipped |
| PostgreSQL + MinIO 本地备份恢复演练 | passed，恢复后集成测试再次通过 |
| `npm run check` | passed：ESLint、6 个控制台测试、Vue 类型检查、控制台构建、TypeScript SDK 检查 |
| Ruff 与 Mypy | passed |
| OpenAPI 与 TypeScript SDK 生成漂移检查 | passed |
| `python scripts/repository_gate.py` | passed |
| `python scripts/release_gate.py --implementation-only` | passed |
| 严格 `python scripts/release_gate.py` | 按设计失败关闭：许可证仍为工程占位文本，且尚无正式 `manifest.json` 与九类签署报告 |
| `python -m pip_audit -r requirements/dev.txt` | 未发现已知漏洞 |
| `pnpm audit --audit-level high` | 未发现已知漏洞；锁文件将 `brace-expansion` 固定到已修复的 5.0.8 |
| `bash -n deploy/scripts/*.sh` | passed；使用缓存的 Linux 容器执行 |
| 本机控制台镜像阶段 | 未执行完成：Docker Desktop 代理 `127.0.0.1:10808` 无法拉取官方 Node 基础镜像；同一构建已加入 GitHub CI |
| 浏览器桌面端验收 | 12 条路由连接后端，无 HTTP 错误、无控制台错误、无页面横向溢出 |
| 浏览器移动端验收 | 12 条路由连接后端，无 HTTP 错误、无控制台错误、横向溢出均为 0 |

### 执行环境与追溯

- 基线提交：`ab5d7deac90187b1714bb13954a6d9662e2040e1`。
- 本机环境：Windows 11 build 26200、Python 3.12.3、Node.js v24.13.1、npm 11.8.0、pnpm 11.9.0、Docker Engine 29.1.3、Docker Compose 5.0.0。
- 本机 Node.js 版本高于正式支持的 22.22.2，pnpm 引擎检查会提示警告；正式 CI 与发布验收必须使用支持矩阵版本。
- `pnpm-lock.yaml` SHA-256：`50308c9fd432c9147d4f5e3efe4ea889262a4f633c78a25cf01d4baa540a2e07`。
- `requirements/production.lock` SHA-256：`a44e7e836e1effe09bdcbfa75e9c7c19c1bbd2acd09186fa37e73d4dbfbefda4`。
- `requirements-cpu.lock` SHA-256：`5dab4913da174b3d936d8dfc3f1660882d1b96b7b5e60128e93c6ec5698e4777`。
- Python `pip-audit` 与项目采用的 pnpm 锁文件审计均未发现已知漏洞。

### 发布边界

本次更新完成的是升级方案中当前仓库可实施的开发、自动化、模板和本地验证工作，不等同于 Scenara 1.0 已获得生产发布资格。只有 `docs/release/evidence/manifest.json` 聚合的 `backup_restore`、`gpu_capacity`、`integration_services`、`model_rights`、`ocr_evaluation`、`offline_install`、`portrait_evaluation`、`security_assessment` 和 `software_license_approval` 九类报告全部绑定同一发布身份，由责任人执行、校验和签署，并通过严格发布门禁后，才能将对应能力标记为 Qualified 或 Released。
## 2026-07-30：补充验证与遗留说明

- Python 覆盖率门禁复验通过，结果为 `81 passed, 5 skipped, 77.12% coverage`。
- `SCENARA_RUN_INTEGRATION=1 python -m pytest -q -m integration tests/integration` 复验通过，结果为 `5 passed`。
- `npm run check` 复验通过，前端构建、单测、类型检查和 TypeScript SDK 检查均通过。
- `python -m ruff check .`、`python -m mypy --explicit-package-bases scenara sdk/python/scenara_sdk scripts`、`python scripts/release_gate.py --implementation-only` 均通过。
- `python -m pip_audit -r requirements/dev.txt` 通过，未发现已知漏洞。
- `pnpm audit --audit-level high` 通过，未发现已知漏洞。
- `npm audit --audit-level high` 在当前 workspace 安装树上仍会报出旧的依赖噪声，因此本次发布结论以 `pnpm audit` 为准。
- 严格 `python scripts/release_gate.py` 仍然按设计保持失败关闭，因为软件许可证法务批准和正式的九类签字证据尚未到位；这属于生产资格边界，不是代码合并错误。
- 生产 Compose 现在对 API、Worker 与调度器统一挂载只读私有模型目录，并强制提供 `SCENARA_OCR_ENGINE_FACTORY`；示例加密密钥改为不可直接使用的生成提示，防止示例值被误用于生产。
- 模型发布证据从“非空引用”升级为对象存储实体验证：校验租户/项目作用域、对象 SHA-256、模型标识、版本、模型包摘要、证据类型、签署人、时区时间和分类明细；权属、领域评估与回归证据缺失时禁止状态迁移和回滚。
- 仓库门禁扩大到环境变量示例、Dockerfile、Shell 与 PowerShell 脚本，识别令牌、加密密钥和已知全零 Fernet 密钥，同时正确处理空值与明确占位值；新增对应回归测试。
- 明确发布证据的信任边界：仓库门禁验证报告摘要、结构一致性和具名审批，不冒充密码学身份认证；审批身份及外部签名链由受控发布流程验证和留存。

## 2026-07-30：产品矩阵仓库拓扑落地

- 正式确定当前 `scenara` 仓库为平台集成仓库，不改名为 `scenara-parse`；统一承载 Scenara Parse、Console、API、SDK、共享 IAM/授权/审计、平台运行时以及 Model/Data 的平台侧集成治理。
- 将已有模型训练仓库定义为逻辑标识 `scenara-model` 的专业仓库，负责训练作业、实验跟踪、训练算力调度、训练评估和不可变模型制品；模型准入、发布、激活、回滚和部署审计继续由当前仓库负责。
- 将 `scenara-data` 定义为规划中的专业仓库；在数据集版本、标注审核、质量、血缘、授权、导出和负责人形成稳定边界前不创建空壳仓库，业务媒体、结果、反馈和难例导出继续留在当前平台。
- 新增 `GET /api/v1/platform/repositories` 仓库拓扑契约，公开仓库生命周期、负责与排除职责、四类跨仓库契约以及禁止共享数据库、禁止跨仓库源码导入、仅使用版本化契约和不可变制品引用四项强制规则。
- Python SDK 增加 `get_repository_topology()`，TypeScript SDK 增加 `getRepositoryTopology()`；OpenAPI、生成类型和发布构建制品同步更新。
- Console 总览增加全中文仓库拓扑视图，展示当前仓库、已有 Model 仓库和规划 Data 仓库的职责、非职责、下一道门禁、跨仓库契约与强制边界，并新增英文后端文案泄漏回归测试。
- 新增 `docs/strategy/REPOSITORY_TOPOLOGY.md`，补齐职责矩阵、集成流程、Data 拆分门禁和变更要求；产品矩阵、README、发布说明与实施矩阵同步更新。
- 修复密钥扫描将公开的 `OPENAPI_SHA256` 契约摘要识别为泄漏的问题：生成器仅在该公开摘要常量同行写入 `gitleaks:allow`，未增加文件级或规则级宽泛白名单。
