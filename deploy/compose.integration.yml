services:
  postgres:
    ports:
      - "127.0.0.1:${SCENARA_INTEGRATION_POSTGRES_PORT:-55432}:5432"

  redis:
    ports:
      - "127.0.0.1:${SCENARA_INTEGRATION_REDIS_PORT:-56379}:6379"

  minio:
    ports:
      - "127.0.0.1:${SCENARA_INTEGRATION_MINIO_PORT:-59000}:9000"

